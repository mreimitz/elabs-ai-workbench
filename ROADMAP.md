# MCP Token Footprint Roadmap

> **This file is a pointer, not a plan.** The live roadmap is generated: every `RM` initiative with
> its status, every `RS` research topic and every `DC` documentation subject are listed in
> [`planning/Roadmap/roadmap.md`](./planning/Roadmap/roadmap.md), regenerated by `pnpm okf:sync`.
> Per-item work-package state lives in that item's own `STATUS.md` ledger; finished initiatives move
> to [`planning/Roadmap/completed/`](./planning/Roadmap/completed/). What the app does **today** is
> the capability table in [`CLAUDE.md`](./CLAUDE.md) and the front page in [`README.md`](./README.md).
>
> The narrative phase plan this file used to carry described the original startup-footprint MVP and
> presented much of what has since shipped as future work. It is preserved as a historical record in
> [`planning/Roadmap/completed/RM-31-mvp-footprint-analyzer/`](./planning/Roadmap/completed/RM-31-mvp-footprint-analyzer/).
>
> The lifecycle every piece of work follows — planned as an `RM` item, built against its ledger,
> recorded in a `DC` subject, retired with `complete-roadmap` — is **§11 of
> [`CLAUDE.md`](./CLAUDE.md)**.

## Vision

A local web app for **analyzing MCP servers**: connect to one or many, extract their full tool
surface, measure both the **startup definition footprint** and the **runtime call cost** in
model-context tokens, track changes over time, compare servers against each other, and exercise
individual tools through schema-generated forms.
