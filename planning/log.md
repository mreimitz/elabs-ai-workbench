# Project Knowledge Update Log

## 2026-08-20

* **Structure**: Installed the Open Knowledge Format planning bundle at `planning/`, with `Research/`, `Roadmap/` (+ `Roadmap/completed/`) and `user-guide/` as its three tagged domains.
* **Profile**: Named the documentation root `user-guide` so a `DC-NN` subject holds both the delivery record and that part of the manual; registered `Guide Page`, `Work Package Spec` and `Status Ledger` so the workbench's existing artifacts validate in place.
* **Lifecycle**: Gated `complete-roadmap` on each item's `STATUS.md` work-package ledger — the format `/next-wp` maintains — instead of the upstream scaffold's `tasks.json` boards.
