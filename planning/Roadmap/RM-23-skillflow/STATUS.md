---
type: "Status Ledger"
title: "SkillFlow \u2014 work-package status ledger \u00b7 PRIORITY: HIGH"
description: "Living state for the SkillFlow plan, read and updated by /next-wp skillflow. It picks the next"
tags: ["roadmap", "RM-23"]
timestamp: "2026-08-20T13:47:37Z"
status: "active"
---
# SkillFlow — work-package status ledger · **PRIORITY: HIGH**

Living state for the **SkillFlow** plan, read and updated by `/next-wp skillflow`. It picks the next
**open** WPs whose **dependencies are done**, runs them with parallel worktree sub-agents, and ticks
a box **only** when that WP's Acceptance is met and the gate
(`pnpm typecheck && pnpm test && pnpm build && pnpm lint`) is green.

**Legend:** `[ ]` open · `[x]` done. A trailing `status:` note marks `in progress` / `in review` /
`blocked`. Done lines record date + branch: `… — done <YYYY-MM-DD> · wp/skillflow/<id>`.

> All decisions are locked — see [`00-architecture.md`](./00-architecture.md) (D1–D8).
> Recommended first slice: `1.0 → 1.1 → 1.3` (with `1.2 ∥ 1.1`), then the Trace slice
> `2.1 → 2.2 → 2.3`, then fan out `3.1 ∥ 4.1`.

> **Entry precondition:** the skills plan's deferred owner-acceptance items
> ([`../skills/STATUS.md`](/Roadmap/RM-24-skills/STATUS.md) — Phase 1 & Phase 2 visual/a11y walks) must be
> closed **or explicitly waived by the owner (with a note here)** before WP 1.0 starts.
> Waived/closed: **waived by owner 2026-07-03** — go given for SkillFlow implementation; the
> skills-plan Phase 1/2 visual/a11y walks remain open in `../skills/STATUS.md` and should still
> happen, but no longer block this plan.

## Phase 1 — Foundation: contracts, graph projection, blank skill, read-only Design tab
- [x] WP 1.0 — shared contract (graph IR + trace vocabulary + session-trace shape) — done 2026-07-03 · a38d2ab. All three schemas as types+zod+constants in `packages/shared` (graph IR w/ 5-kind discriminated union + anchors; 8-type trace-event union; session-trace + alignment + verdicts w/ nodeId-or-edgeId refinement); route/response schemas + additive `blank` member on `skillImportSchema`; version stamps + session caps + loop threshold constants. 11 contract tests incl. fixture `run_steps→TraceEvent[]` adapter. Gate green (378 tests). Shared has no test runner — tests live at `apps/api/test/skillflow-contract.test.ts`.
- [x] WP 1.1 — SKILL.md → graph projection engine + `GET …/graph` route — done 2026-07-03 · wp/skillflow/1.1 (52748cf, merge 16d2149). Pure deterministic `projector.ts` (headings→subroutines incl. H1 + nested `###`; file refs→assets; script refs+verify language→validation_gates; branching prose→gatekeepers w/ condition edges; retry language→loop_guards w/ maxIterations) + `annotations.ts` (`<!-- skillflow:gatekeeper|gate id=… -->` overrides, `source:'annotated'`, stable ids) + `GET /api/skills/:id/versions/:vid/graph` (404s; invalid SKILL.md → empty graph + warnings, not 500). 15 tests over the real fixture corpus incl. determinism + anchor checks. Doc'd deviations: H1 counts as a section; annotated-gate script resolution falls back to nearest preceding script ref. Gate green (393 in-worktree).
- [x] WP 1.2 — blank-skill creation (API `source:'blank'` + wizard third source) — done 2026-07-03 · wp/skillflow/1.2 (84b2fa2). `scaffold.ts` (deterministic minimal spec-valid SKILL.md; name validated per manifest rules → 400) + blank branch in skills routes via `SkillRepository.createVersion` (same building blocks as upload ingestion; storage mapping `source_type/source_kind='upload'`, `imported_from='upload'`, `source_ref='blank'` — no CHECK change) + wizard third source card + `createSkillFromBlank` helper; orchestrator follow-up wired `onCreateBlank` in `App.tsx`. 10 tests. Gate green on merged wave (403 tests). ⚠ OWNER-VERIFY: wizard visual walk, both themes @ localhost:8080.
- [x] WP 1.3 — web: Design tab — read-only canvas on `@elabs-ai/components-flow` — done 2026-07-03 · wp/skillflow/1.3 (0a0cceb, merge 69f02ad). First `@elabs-ai/components-flow` consumer: `SkillDesignView` (CanvasShell/FlowNode/FlowEdge/Legend/ZoomControls; loading/error/empty per rules), deterministic layered `graph-layout.ts` (sections main-column by anchor line; assets/gates/loop-guards side-band; condition labels via a composed non-interactive label node — FlowEdge renders no labels itself), `NodeDetailPanel` (InspectorPanel + anchored SKILL.md excerpt), Design tab wired into `SkillInspector`. `@xyflow/react` added as explicit web dep (same range `@elabs-ai/components-flow` already ships/resolves — needed for its required root stylesheet import; pnpm phantom-dep hygiene, no new download). Smoke-tested against the real built app via Playwright: all four fixture skills + a blank skill render; node click + keyboard (Tab/Enter) selection verified; both themes screenshotted, zero console errors. Gate green.

## Phase 2 — Trace Mode over internal runs
- [x] WP 2.1 — run→trace normalizer + `run_skills` persistence — done 2026-07-03 · wp/skillflow/2.1 (0777c5d). `run_skills` table (denormalized skill refs per run_steps.server_id immutability stance; migration v9) + `recordRunSkills` at run resolve; `run-trace.ts` normalizer (llm_response→turn via assistant_text, read_skill_file→skill_file_read from real payload.args shape, tool_result carries status/bytes; llm_request/context_event skipped, idx gaps intentional); `GET …/versions/:vid/runs` (RunSummary[]) + `GET /api/runs/:runId/trace` (additive `runTraceResponseSchema` — no fake alignment); ambiguous multi-skill → 400, old runs → 404 w/ reason. **Key ground truth for WP 2.2: persisted tool_results carry NO structured exit codes** — available signals are tool_name, status ok/error, result_bytes, timing; `script_result` events arrive only via session ingestion (WP 3.1). Gate green (407 tests).
- [x] WP 2.2 — alignment engine (deterministic signals → verdicts) — done 2026-07-03 · wp/skillflow/2.2 (ad67c2c, merge d506f72). Pure `aligner.ts`: skill_file_read→asset visits; script_result exit codes→gate ok/fracture; tool_result-status degradation only over a deterministic link (else gate honestly `unvisited` — asymmetric: linked errors can fracture, ok is never gate-pass evidence); marker gateId/routeId exact matching w/ expected-vs-actual fracture; no-marker gatekeepers conservative (never fractured without markers); loop detection (threshold + loop_guard maxIterations, derived section visits included); one verdict per node (fracture>ok>unvisited); unmatched events reported. `GET …/versions/:vid/trace?runId=…` (400/404/409-on-version-mismatch; sessionTraceSchema-validated, both version stamps). 12 fixture-driven tests; zero shared changes. Gate green.
- [x] WP 2.3 — web: Trace tab — overlay, run picker, evidence pane — done 2026-07-03 · wp/skillflow/2.3 (62c62a0). Shared canvas extracted to `SkillGraphCanvas.tsx` (Design + Trace render ONE implementation; also fixed a latent WP 1.3 bug — React Flow v12 controlled-nodes meant click-select never stuck; `useNodesState` fix makes Design's detail panel work too). Overlay: verdict→FlowNode tone (ok→success, fracture→destructive, unvisited→default+opacity-40), ×N visit Badges, fracture reason in node subtitle, traversal counts + animated dash on traversed edges, verdict-extended Legend + summary strip; `SkillTraceView` (run picker, guidance EmptyState, 409→inline ErrorState w/ server message) + `TraceEvidencePane` (per-verdict evidence filter+highlight, collapsible unmatched, link to run console). Smoke-tested on the real built app w/ seeded fracture run: 16/16 DOM checks, both themes screenshotted. Deep-link query param skipped (tabs aren't routed); gatekeeper expected-vs-actual markers await WP 3.2. Gate green (442).

## Phase 3 — External session ingestion — **REMOVED by owner decision 2026-07-03** (session logs must be the app's own test-run logs; see D6 amendment in `00-architecture.md`. WP 3.1's ingestion surface was removed via migration v12; WP 3.2's breadcrumbs/confidence work is source-agnostic and stays fully active on internal runs.)
- [x] WP 3.1 — Claude Code session JSONL ingestion → shared trace vocabulary — done 2026-07-03 · wp/skillflow/3.1 (0dbc63d). `session_traces`/`session_trace_events` (denormalized skill refs, cascade delete; migration v10) + `session-ingest.ts` line-by-line parse (unknown types/malformed lines → warnings, never fatal; caps enforced DURING parse → 413 reject-not-truncate, nothing persisted) + conservative exit-code inference (`exit code N` text > `is_error`→1 > plain tool_result — never forges exit-0) + `redactSecrets` (provider-key patterns + Bearer/api_key/long-blob; tool args & result bodies never persisted) + session CRUD routes + trace route extended runId-XOR-sessionId (409 on version mismatch, source `session_upload`). `SESSION_MAX_*` env caps. **End-to-end confirmed: fixture session's validate.py exit-1 fractures the gate through the unchanged aligner.** 11 tests; gate green (442). Web source-picker deferred to post-2.3 follow-up (API complete).
- [x] WP 3.2 — breadcrumb convention + gatekeeper verdict hardening — done 2026-07-03 · wp/skillflow/3.2 (3bb4d63). `breadcrumb-convention.md` spec (`[skillflow:gate=<nodeId> route=<edgeId>]` standalone line; copy-paste SKILL.md instruction; degradation story; token-cost note) + shared `SKILLFLOW_MARKER_PATTERN` + one `markers.ts` parser feeding BOTH normalizers (run-trace markers get fresh idxs after the max step idx — evidence is array-membership only, verified safe); aligner verdicts gain additive `confidence: 'exact'|'inferred'` (exact = marker/script_result-backed evidence; ALIGNER_VERSION 1→2 per version-stamp rule); annotation id stability across section reorder tested. PLUS deferred WP 3.1 web piece: Trace-tab source toggle (Test runs | Uploaded sessions), `SessionUploadPanel` (FileUpload dialog, delete-confirm), session trace via `?sessionId=`. 17 new tests. Gate green (459). ⚠ session-picker UI not visually walked (Phase-3 owner acceptance).

## Phase 4 — Design editing (round-trip)
- [x] WP 4.1 — graph-edit → SKILL.md round-trip engine (anchors, new immutable version) — done 2026-07-03 · wp/skillflow/4.1 (1383b51, merge d506f72). 9-op vocabulary (shared, additive) + `edit-ops.ts` semantic validation (unknown ids/conflicts→400) + `roundtrip.ts` pure anchor-scoped line splicing (bottom-up, no markdown re-serialization; accessory-remove + unanchored-condition edits degrade to warning+skip, never corruption; subtree/body span choices documented incl. neighbor-annotation guard). `POST …/versions/:vid/edits`: baseTreeSha stale→409, `{unchanged:true}` on no-op, else new immutable version via the WP 1.2 ingest path (`source_ref='skillflow-edit'`) + WP 1.5 diff. Byte-exactness proven mechanically in tests (returned splices reproduce the result; all lines outside spans string-identical; deletions confined to targeted anchors) across all four fixtures. 12 tests. Gate green on merged wave (431 tests; routes.ts both-blocks conflict resolved by orchestrator).
- [x] WP 4.2 — web: Design-tab editing via `InspectorPanel` + save-as-new-version — done 2026-07-03 · wp/skillflow/4.2 (d28d4e5, merge dafafd5). `use-edit-ops` buffer (client-side conflict rules mirror the server; same-type op replaces; pure best-effort preview transform — API stays authoritative) + per-kind panel editors (rename/body via @elabs-ai/components-editor/gate expectation/edge condition/annotation/remove w/ accessory guard) + toolbar "Add section" + `SaveVersionDialog` (note, op summary, diff review; 409 → in-dialog Alert + Reload-graph keeping the op buffer; 400/unchanged handled) + edit-mode toggle w/ unsaved-changes guards; blank skills open editing by default. 7 of 9 ops UI-reachable (`add_asset_ref`/`reorder` deferred — no natural panel affordance; flagged). Both end-to-end loops proven live: blank→design→v2; uploaded→one-section edit→diff shows SKILL.md only. Two real bugs found+fixed (selection dropped on preview rebuild; premature version-switch unmounting the dialog). Both-theme screenshots. Gate green on merged wave (459 tests).

## Phase 5 — Gates as first-class tests + the feedback loop
- [x] WP 5.1 — validation-gate expectations unified with `tests.assertions_json` — done 2026-07-03 · wp/skillflow/5.1 (fa74c5f). `TestAssertions` (skillGates pass/visited · skillRoutes · noFractures) + `AssertionResult` (pass/fail/**unevaluable** — misconfigured assertions surface, never silently pass/fail) activated on the reserved `assertions_json` column; pure `assertions.ts` evaluator over alignments (confidence/evidence propagate); run-completion hook evaluates only on `completed`, fully crash-guarded (never throws into the engine), persists to additive `runs.assertion_results_json` (migration v11). **Owner decision RESOLVED 2026-07-03: dedicated `assertions_failed` outcome added** (follow-up commit — flips only when status+outcome were both `completed`; engine outcomes never masked; every outcome consumer updated). Web: `AssertionsEditor` on tests (graph-fed pickers) + `AssertionResults` badges on the run console (deep-link to /skills/:id; Trace tab unrouted per WP 2.3). 12 tests. Gate green (471).
- [x] WP 5.2 — trace → suggested SKILL.md edit (feedback loop) — done 2026-07-03 · wp/skillflow/5.2 (338ff9a; salvaged by orchestrator after the implementing agent hit a session limit post-gate — full gate re-verified before commit). Pure deterministic `suggestions.ts` (no model calls — LLM branch stays owner-gated/unbuilt): 5 rules keyed on verdict STRUCTURE (missing-breadcrumbs → set_annotation + body-append of the WP 3.2 instruction sentence; loop-detected → bounded-retry body-append; asset-never-visited / gate-failed-consistently / marker-route-mismatch → advisory-only, ops []); every non-empty op batch `validateEditOps`-checked against the same graph, failures downgraded to advisory (no-corruption guarantee); deterministic ids (rule:nodeId). `GET …/versions/:vid/suggestions?runId=|sessionId=` mirrors trace-route semantics; `SuggestionCard` in the Trace evidence pane w/ Review-&-apply through the WP 4.2 save-dialog flow (never auto-applies). 8 tests incl. round-trip byte-safety of applied suggestions. Gate green (479). **Phase 5 complete — plan done** (owner-acceptance walks remain, see below).

## Owner acceptance (deferred visual / a11y — owner-only, not subagent-doable)

> **Rule (inherited from the skills plan):** a new phase must **not** open while a prior phase
> still has unresolved owner-acceptance items here. Close (or explicitly waive, with a note)
> before starting the next phase.

- [ ] Phase 1 (WP 1.3) — two-theme visual walk of the Design tab across an uploaded skill, a
      GitHub-imported skill, and a fresh blank skill @ localhost:8080 — accepted: ____
- [ ] Phase 2 (WP 2.3) — two-theme visual walk of the Trace tab against a real run with an
      attached skill (green path, a fracture, a never-visited node) @ localhost:8080 — accepted: ____
- [x] Phase 3 (WP 3.1–3.2) — ~~ingest a real Claude Code session JSONL and verify the overlay~~ —
      obsolete: the upload feature was removed by owner decision 2026-07-03; markers/confidence
      (WP 3.2) are covered by the Phase-2 walk on internal runs.
- [ ] Phase 4 (WP 4.2) — edit a skill visually, save as new version, verify the `SKILL.md` diff
      preserves hand-written prose — accepted: ____
- [ ] Phase 5 (WP 5.1–5.2) — gate verdicts appear in test results; a suggested edit round-trips —
      accepted: ____
