# Server types — a first-class grouping for MCP servers

## Concepts

* [Server types — a first-class grouping for MCP servers](item.md) - Give MCP servers a first-class type, so servers sharing a tool surface and configuration shape can be grouped, filtered and bound to as a type rather than one server at a time.
