# Unified Sessions — one session experience across every run backend

## Concepts

* [Unified Sessions — one session experience across every run backend](item.md) - Give the three run backends one session contract: a shared terminal table, an additive ended terminal state, a stall-based clock with no default wall cap, a persisted phase, a capability manifest, one status module and cursor-resumable streaming.
