# Release & delivery — the offline hand-off bundle

## Concepts

* [Release & delivery — the offline hand-off bundle](item.md) - Package the app so someone with only Docker Desktop, no repository access and no registry can run it: a saved image plus self-detecting launchers in one directory.
