---
type: "Status Ledger"
title: "Interface Craft \u2014 work-package status ledger \u00b7 PRIORITY: HIGH"
description: "Living state for the interface-craft plan (source"
tags: ["roadmap", "RM-15"]
timestamp: "2026-08-20T13:47:37Z"
status: "active"
---
# Interface Craft — work-package status ledger · **PRIORITY: HIGH**

Living state for the **interface-craft** plan (source:
`docs/INTERFACE-REVIEW-2026-07-25.md`; 15 findings). Read
and updated **only by the PM/owner** (via `/next-wp interface-craft`); sub-agents never edit this file.
A box is ticked **only** when the WP's Acceptance is met — **with its measured numbers** — and the gate
(`pnpm typecheck && pnpm test && pnpm build && pnpm lint`) is green on `ui/interface-craft` after merge,
validated by the PM, not taken on the agent's word.

**Legend:** `[ ]` open · `[~]` in-flight (agent spawned; note worktree) · `[x]` done — record date +
branch: `… — done <YYYY-MM-DD> · wp/interface-craft/<id>`. Base branch: **`ui/interface-craft`** (cut
from the merged `main`; owner decides the merge to `main`).

**Batch map:** README §Parallel execution map (G → H → I → J → K). **Domains:** the phase files are
authoritative for each WP's file domain — the collision check the PM schedules by.

## Phase 0 — Independent structural fixes ([`phase-0-independent.md`](./phase-0-independent.md)) — Batch G · 4 parallel
- [x] WP 0.1 — Token contrast + semantic split + root type rendering (findings 1, 11-tokens, 15) — done 2026-07-25 · wp/interface-craft/0.1 · app.css per-theme `[data-theme]` override (after the `@elabs-ai/components-tokens` import → wins the cascade; vendored `themes.css` untouched): bright `--primary` L 0.553→0.535 (**4.29→4.60**) · `--info` 0.60→0.55 (**3.74→4.61**); `--success` split to `oklch(0.51 0.15 150)` (≠`--primary`, 5.10) · `--ring` split to `oklch(0.62 0.16 250)` (≠`--info`); dark `--destructive-foreground`→dark ink (**3.02→5.50**) + `--success`/`--ring` split; `body { -webkit-font-smoothing: antialiased }`. **`tokens-contrast.test.ts` (14) asserts all 5 role⇄fg pairs ≥4.5 × both themes + both splits, fails-red on the un-overridden base.** Live rendered-pair + macOS smoothing = 4.2 walk.
- [x] WP 0.2 — FieldRow error association (finding 4) — done 2026-07-25 · wp/interface-craft/0.2 · `FieldRow` injects `aria-describedby={#id-error}` + `aria-invalid` onto the first element child via `Children.toArray` (handles multi-child/Fragment fields), merges a caller-set `aria-describedby`. **28 error-bearing call sites (of 116 across 24 files) now inherit it (was 0 via FieldRow).** Test 6/6. SR-announcement structural-only (not AT-tested).
- [x] WP 0.3 — Shell landmarks + skip link (finding 3) — done 2026-07-25 · wp/interface-craft/0.3 · 4 `app-shell-main` `<main>` variants → `<div>`; `SidebarInset` is the single `<main id="main-content" tabIndex={-1}>`; sidebar → `<nav aria-label="Sections">`; skip link is first-focusable (`sr-only focus:not-sr-only`, `bg-primary`, `focus-visible:ring-ring`, `#main-content`). **`AppShell.test.tsx` (5): `querySelectorAll('main')===1` (was 2), named nav, skip-link-first, focusable target.** Live keyboard 22→1 + focus-ring visibility = 4.2 walk.
- [x] WP 0.4 — Runs feed toolbar adopts D-TB7 (finding 2; migrated Phase-0.a) — done 2026-07-25 · wp/interface-craft/0.4 · hand-rolled `overflow-x-auto [scrollbar-width:none]` wrapper **DELETED** → `ViewToolbar` D-TB7 `flex-wrap` owns the row (`RunFilterBar` `shrink-0`→`flex-wrap` too); Show-forks `Button variant="default"` → `Toggle` (`bg-accent`+`border-primary` when pressed, distinct from the `bg-primary` New-run action). `RunsView.test.tsx` (6). Live 1100px `scrollWidth===clientWidth` (was 160 vs 498) = 4.2 walk (flex-wrap ⇒ no horizontal overflow by construction).

**Batch G closed 2026-07-25** — all 4 merged to `ui/interface-craft` (tip `10cc898`); **full gate GREEN** (typecheck · web **2715** / 275 files · the 4 new WP tests **31/31** · lint 1395 · build 25.5s; API untouched — web-only diff). **PROCESS INCIDENT (fixed, recorded for future batches):** the Agent-tool `isolation: "worktree"` did **not** create isolated worktrees for these four agents, and the dispatch briefs hard-coded the absolute **main**-repo path, so all four ran `git checkout -B` in the **shared main working tree** and raced HEAD (0.2 caught its own commit landing on `0.4`'s branch and self-corrected). No work was lost — the domains are disjoint, so the PM **reconstructed each WP's commit by file domain** deterministically (only 0.2 had committed; 0.1/0.3/0.4's complete edits sat uncommitted on disjoint files), then validated on a throwaway integration branch before merging. **Fix for Batch H+:** the PM creates explicit `git worktree add` dirs per WP and scopes each agent to its **own** worktree path — never the absolute main-repo path — so isolation does not depend on the Agent tool honoring `isolation:"worktree"`.

## Phase 1 — Structure ([`phase-1-structure.md`](./phase-1-structure.md)) — Batch H · 4 parallel · enter after G merged
- [x] WP 1.1 — Section headings are semantic (finding 6, D-IC5) — done 2026-07-25 · wp/interface-craft/1.1 · `SectionCardTitle` wrapper (real `h2`/`h3` via `level`, **byte-identical `text-title` className** to `@elabs-ai/components-ui` CardTitle); `ScansTab` 5 sections → h2, `ServersView` 4 Overview sections → h2 (also fixed a latent h1→h3 skip). **Measured jsdom outline: Dashboard `h1 → 5×h2` (the five sections), Servers `h1 → 4×h2`, Runs one `sr-only` h1 + no sub-sections (no drift).** Tests 4 + 24 targeted. Live running-app outline + both-theme parity = 4.2 walk.
- [x] WP 1.2 — Live regions for streaming + counts (finding 7) — done 2026-07-25 · wp/interface-craft/1.2 · `ResultCount` = native `<output>` (implicit `role="status"` + `aria-live="polite"`/`aria-atomic`, **stable node across re-render — asserted `toBe`, not a remount**); `RunConsole` transcript → `role="log" aria-live="polite"` (AgentTranscript pattern); **6 count sites** converted (ScansView · dashboard FilterControls · EnvironmentsView · CollectionsView · IssuesFleetTab · RunsView). 65 targeted tests; no streaming regression (RunConsole 15/15). SR-announcement structural-only; live-run SSE announce = owner (provider key).
- [x] WP 1.3 — Focus visibility + `inert` (finding 8) — done 2026-07-25 · wp/interface-craft/1.3 · `focus-visible:ring-2 ring-ring ring-offset-2` on the RunLauncher (`launcher-tests`/`-environments`) + SuiteEditor (`suite-scenarios`) validation targets; **closed MetaRail → `inert`** (replaces `aria-hidden`+`pointer-events-none`; removes pointer AND tab access in one attr). MetaRail.test 11 (every focusable has an `inert` ancestor when closed; none `aria-hidden`). **PM touch-up:** `AssistantView.test.tsx` `aria-hidden`→`inert` (cross-domain fallout — out of 1.3's domain). Live ring visibility + real 0-tabbable count = 4.2 (jsdom does not enforce `inert`).
- [x] WP 1.4 — Prose measure caps (finding 9, D-IC9) — done 2026-07-25 · wp/interface-craft/1.4 · `max-w-[68ch]` on the Compatibility callout (**the 190ch line**), assistant markdown segments, and the SKILL.md block; new `ProseCardDescription` wrapper (upstream gap #5). MetricStrip/tables left full-width (acceptance #3). Targeted green. **Live ch/line at 1600px both themes = 4.2 walk** (jsdom has no layout). Flagged: `ChatMarkdown` embedded-table narrowing (still scrolls — acceptable); a pre-existing `@elabs-ai/components-ui` `CardDescription` tailwind-merge quirk (see upstream-gaps #6).

**Batch H closed 2026-07-25** — all 4 merged to `ui/interface-craft` (tip after ticks); **full gate GREEN** (typecheck · web **2728** / 278 files · API **Done** · lint 1401 · build 24.7s). **The corrected isolation WORKED** — 4 PM-made explicit worktrees (`ic-1.1`…`ic-1.4`), each verified on its own branch, zero shared-tree race. One PM integration touch-up (`AssistantView.test.tsx` `aria-hidden`→`inert`, the cross-domain fallout of 1.3's finding-8 fix). Agents respected domains and flagged out-of-domain items (SkillOverview taxonomy-guard drift, `ChatMarkdown` tables, the CardDescription tw-merge quirk) rather than reaching.

## Phase 2 — Consistency ([`phase-2-consistency.md`](./phase-2-consistency.md)) — Batch I · 4 parallel · enter after H merged
- [x] WP 2.1 — Truncation recovery, remainder (finding 10, D-IC10; incl. CrewCard from 0.b) — done 2026-07-25 · wp/interface-craft/2.1 · `RunTableRow` Environment cell + `CrewCard` description now carry `title` (AgentCard verified unchanged); `TitledSelectTrigger` wrapper shipped (upstream gap #4 app-side; required `selectedLabel` so it can't be forgotten, opt-in HoverCard). **3 concrete sites recovered** (82/249 → ~85/246). 60 targeted. **Follow-up (out of this WP's domain):** none of 2.1's three files render a `<Select>`, so the wrapper isn't yet wired to a clipping site — the real clipping selects (e.g. `CompatibilityView:545`) need a separate adoption sweep. Live recoverable-count sweep = 4.2.
- [x] WP 2.2 — One delta convention (finding 11-deltas, D-IC3) — done 2026-07-25 · wp/interface-craft/2.2 · `lib/delta.ts` is the **single authority** (`deltaOutcome(delta, higherIsBetter)` + `DELTA_TEXT_TONE`/`_BAR_TONE`/`_CELL_WASH`/`_BADGE_VARIANT`); all **6** surfaces routed; **worse → `text-warning-text` (amber) everywhere** (was red on the 5 Compare surfaces), **better → `text-success-text` incl. SuiteDeltas** (its `text-primary` bug fixed); structural added/removed stays green/red (D-UX9's separate axis, untouched); `DeltaMatrix` warning icon preserved. Grep-proof single authority; 125 targeted. Corrected drifted paths (`features/testing/compare/**`). **Owner note:** consolidates D-UX9 to amber-worse — the Compare summary chip is now amber, not red (supersedes D-UX9's "magnitude-red on summary" via D-IC3); **flagged for the 4.2 visual walk**.
- [x] WP 2.3 — Filter and search empty states (finding 13) — done 2026-07-25 · wp/interface-craft/2.3 · **5** hub filter/search dead-ends now echo the query + offer a wired Clear-filter exit: `ProjectLibraryPanel`, `CrewLibraryPanel` (+ its `description: undefined` bug), `RoleLibraryPanel`, `DirectoryTab`, `SessionBreadcrumbSwitcher`. 57 targeted; blast radius exactly 10 files (~114 good empty states untouched). **Drift:** only 5 (not the review's "6") genuine filter dead-ends live in `features/hub/**`; candidate out-of-hub sites (`ScansView`/`EnvironmentsView`/`ConsolePanel`/`StepLog` — plain `DataTable emptyMessage`, a different pattern) were **reported, not reached** → follow-up.
- [x] WP 2.4 — Card elevation consistency (finding 14, D-IC11) — done 2026-07-25 · wp/interface-craft/2.4 · **21** hand-rolled `bg-card`+border-no-shadow surfaces → `<Card>`/`cardVariants()` (settings · meta-rail · review · skills). Meta-rail hairlines **de-stacked** (`MissionSummary`→Card, `AgentRow` inner border dropped → `bg-background` separates). Settings unified to **one radius** (`rounded-lg`). **No** concentric radius (rejected). 117 targeted. **Gap explained (21 vs ~27):** SettingsView's 7 review-cited sites were already `bg-muted/40` (toolbar-reach restyle → 0 conversions there), plus deliberately-excluded muted/dashed/warning/transparent/selection surfaces — no force-convert-to-number. **Flagged:** `pnpm format` reformats 511 files (biome-version drift — do NOT run `format` at integration; the pollution was contained to 2.4's 19 files); pre-existing `text-xs`/`text-sm` taxonomy-guard warnings in touched skills files (out of domain, left as-is).

**Batch I closed 2026-07-25** — all 4 merged to `ui/interface-craft` (tip `67990c3`); **full gate GREEN** (typecheck · web **2752** / 280 files · API **Done** · lint 1405 · build 24.5s). Corrected isolation held (4 explicit worktrees, zero race); **no PM touch-up needed**. Agents respected domains and flagged out-of-scope items (select-adoption sweep, out-of-hub empty states, the 511-file `pnpm format` drift, skills-typography taxonomy warnings) rather than reaching.

## Phase 3 — Voice ([`phase-3-voice.md`](./phase-3-voice.md)) — Batch J · 3.1 solo → then parallel · enter after I merged
- [x] WP 3.1 — Notification timing (finding 5, D-IC7) — done 2026-07-25 · wp/interface-craft/3.1 · `lib/notify.ts` = the **single error-notification authority** (`notifyError` forces `duration: Infinity` **last**, so a caller can't sneak a finite one through); **176 `toast.error(` call sites across 69 files → `notifyError(`** (the only remaining `toast.error` is the wrapper at `notify.ts:20`); `PromoteToTestDialog` action toast → `duration: Infinity`; `<Toaster>` keeps `duration={4000}` for successes. `notify.test.ts` (4) incl. a `{duration:4000}`→`Infinity` override test. **Mechanical rename only** — the agent's hunk-by-hunk verifier proved zero message-text change; dead `toast` import specifiers dropped from 15 files. Full gate GREEN on merge (web **2756** · API Done · lint 1407 · build 24.7s).
- **WP 3.2 split 4 ways (PM, for load balance — within the cap-of-4, per README "split 3.2 further by directory"):** the opener load is uneven (`hub`~82, `skills`~44, `testing`~63, rest ~26), so `hub` and `skills` run as separate agents. Preserve the target-voice sites: `RunConsole.tsx:882`, `GradePanel.tsx:145` (3.2a), `IconPicker.tsx:141` (3.2b).
- [x] WP 3.2a — One error voice · `components/**` + `features/testing/**` (finding 12, D-IC8) — done 2026-07-25 · wp/interface-craft/3.2a · 40 files; ~50 openers → "Couldn't … + next step" (curly `’`); `ErrorBoundary` "Something went wrong"→"Couldn't render this page.", "Call failed"→"Couldn't run the tool.", "Assertions/Session failed"→"Couldn't …"; target-voice `RunConsole`/`GradePanel` preserved (diff-proof); 4 pre-existing straight-apostrophes fixed; 0 exclamation/Oops/blaming. Full gate green in-worktree (887 targeted + build).
- [x] WP 3.2b — One error voice · `features/hub/**` (finding 12, D-IC8) — done 2026-07-25 · wp/interface-craft/3.2b · 40 files; 82 `Could not`→`Couldn't`; the named `"org rail"`→`"agents & crews"`, `"agent turn failed"`→`"Couldn't finish this turn"`; dead-ends given real next steps (IconPicker, AgentProfileModal); 5 straight-apostrophes fixed; `IconPicker` target-voice preserved; 2 `console.warn` diagnostics left; 925 targeted.
- [x] WP 3.2c — One error voice · `features/skills/**` (finding 12, D-IC8) — done 2026-07-25 · wp/interface-craft/3.2c · 27 files; ~44 `Failed to`/`<Noun> failed`→`Couldn't`; `"Can't connect those"`→`"Couldn't create that connection"`; GitHub push/publish banners rewritten against real API 401/403/409 semantics; diagnostic finding-labels (`"Gate failed"`) + business-rule refusals correctly left; 238 targeted.
- [x] WP 3.2d — One error voice · `features/{settings,servers,scans,review,watch,issues-fleet}/**` (finding 12, D-IC8) — done 2026-07-25 · wp/interface-craft/3.2d · 15 files; ~45 openers normalized; `"Action failed"`→named lifecycle actions; the **SettingsView straight/curly apostrophe inconsistency fixed** (51 curly, 0 straight); one ad-hoc error upgraded to `InlineError`+retry; 186 targeted. Flagged raw `getErrorMessage(error)` pass-through sites (no authored copy — follow-up).
- [x] WP 3.2e — One error voice · uncovered dirs (`App.tsx`, assistant/compatibility/reports/dashboard/compare/notifications/issues) (finding 12, D-IC8) — done 2026-07-25 · wp/interface-craft/3.2e · **PM-added supplemental slice** — the a–d slicing missed these dirs (~44 user-facing openers). 16 files; `"thread"`→`"conversation"`, `"source"`→`"sign-in method"`; `getErrorMessage(cause,"…")` fallbacks + `StatePanel title`s rewritten; `lib/` surveyed clean; status-vocab literals + `console.*` left; target-voice preserved. **App-wide user-facing opener sweep now returns 0.** 554+27 targeted.

**Batch J closed 2026-07-25** — 3.1 + 3.2a–e all merged to `ui/interface-craft` (tip `40f85168`); **full gate GREEN** (typecheck · web **2756** / 281 files · API Done · lint 1407 · build 23.2s). **App-wide error voice unified** — zero user-facing `Could not`/`Failed to`/`<Noun> failed` openers remain (verified by cross-app grep). Isolation held across all 6 agents; no PM touch-up. **PM coverage fix:** the original a/b/c/d slices (from the kickoff) missed `assistant`/`reports`/`compatibility`/`dashboard`/`compare`/`notifications`/`App.tsx` — the PM caught this at integration (app-wide sweep found 27→44 stragglers) and added slice **3.2e** to close finding 12 fully. **Follow-up (non-blocking):** raw `getErrorMessage(error)` pass-through error surfaces with no authored copy (noted by 3.2d) are not one of the four governed voices — a separate wrapper decision.

## Phase 4 — Close ([`phase-4-close.md`](./phase-4-close.md)) — Batch K · 4.1 solo → then 4.2 (owner) · enter after J merged
- [x] WP 4.1 — Guardrails (asserts D-IC1/2/4/6/7 + prose-measure) — done 2026-07-25 · wp/interface-craft/4.1 · **six guardrails, each demonstrated RED→GREEN** (inject the pre-fix pattern → caught → revert → green): `apps/web/src/guardrails/{token-contrast-identity,one-main,field-error,notify-duration,prose-measure}.guardrail.test.*` (39 tests) **+ 2 edit-time hooks** `.claude/hooks/{no-bare-toast-error,prose-measure}.mjs` (registered in `settings.json`). D-IC7 + D-IC9 have **both** a runtime test and a static hook; the **prose hook is proven NOT to false-positive on tables** (`DataTable`/raw full-width table → exit 0) with a `prose-measure-allow` opt-out. No existing test edited, no feature code touched (only `settings.json` modified). Full gate GREEN on merge (web **2795** / 286 files · API Done · lint 1414 · build 23.6s).
- [x] WP 4.2 — Re-run the review (owner acceptance; substitute skill per README) — done 2026-07-25 · PM · **verdict: Approve.** Measured pass (Playwright/Chrome, both themes): all 10 contrast pairs ≥4.5 (four failures 4.31/4.31/3.76/3.02→4.60/5.09/4.61/5.50), `<main>` 2→1, named nav, skip-link-first, `--success≠--primary`/`--ring≠--info`, font-smoothing auto→antialiased, Runs zero-overflow. `brand-ui-visual-ux-reviewer` pass confirmed the 5 HIGH resolved (+ exhaustive every-text-node contrast sweep, 0 sub-threshold outside 2 edge cases) and surfaced 3 new issues → **WP 4.3** fixed them. Report: [`verification-report.md`](./verification-report.md).
- [x] WP 4.3 — Acceptance-walk fixes — done 2026-07-25 · wp/interface-craft/4.3 · **P0** scan-404 dead-end → terminal error `StatePanel` + "Back to scans" (no infinite loading; RunConsoleRoute pattern); **P1** toast `richColors` dropped → tokens (error contrast **4.35→17.93**; renders neutral-not-red — noted follow-up); **P1** OrgRail `text-primary`-on-tint → `bg-primary/10` chip + default fg (**4.28→8.11**), green on the icon; **P2** `--success`→emerald H166 (on-fill 5.45/9.87, oklab dist from `--primary` ≈doubled). All re-measured live; `tokens-contrast.test`+guardrails green; full gate GREEN (web **2796** · build 25.5s).

---

## 🎉 PROGRAM COMPLETE — all 15 findings + guardrails — 2026-07-25
All merged on **`ui/interface-craft`** (cut from the merged `main`; `main` never touched, nothing
pushed). **Full gate GREEN after every batch** (final: typecheck · web **2796** / 286 files · API Done ·
lint 1414 · build ✓ ~25s). **Acceptance re-run verdict: Approve** (measured, both themes —
[`verification-report.md`](./verification-report.md)). What shipped: the six missing rules **written,
applied, and asserted in CI** — on-fill contrast (D-IC1, all pairs AA, guarded), semantic token split
(D-IC2), one delta convention (D-IC3, `lib/delta.ts` amber-worse), shell landmarks + skip link (D-IC4),
semantic section headings (D-IC5, `SectionCardTitle`), field-error association (D-IC6, `FieldRow`),
notification timing (D-IC7, `notifyError`), one error voice (D-IC8, app-wide sweep = 0 off-voice),
prose measure caps (D-IC9), truncation recovery (D-IC10), card elevation (D-IC11) — plus **six CI
guardrails + two edit-time hooks** so the drift can't recur, and the acceptance-walk fixes (P0/P1/P2).
**Remaining = the owner's:** the `ui/interface-craft → main` merge, and the honestly-listed residuals
(data-gated live geometry, SR-announcement, the toast red-plate nuance, and pre-existing follow-ups) in
the verification report's Verdict section.

<!-- One line per WP. Seeded every WP open; never pre-tick. The PM ticks a box only on a validated,
     gate-green merge, appending the date + branch, a one-line what-shipped note, and the measured
     numbers that prove the Acceptance. -->
