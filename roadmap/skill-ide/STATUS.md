# Skill IDE — work-package status ledger · **PRIORITY: HIGH**

Living state for the **Skill IDE** plan, read and updated by `/next-wp skill-ide`. A box is
ticked **only** when that WP's Acceptance is met and the gate
(`pnpm typecheck && pnpm test && pnpm build && pnpm lint`) is green.

**Legend:** `[ ]` open · `[x]` done. Done lines record date + branch:
`… — done <YYYY-MM-DD> · wp/skill-ide/<id>`.

> Decisions locked in [`00-architecture.md`](./00-architecture.md) (I1–I8; SkillFlow D1–D8
> inherited). WP specs live in the `phase-*.md` files — each open WP now carries
> **Implementation notes (verified 2026-07-04)**; the shared code-reality inventory (real APIs,
> constants, fixtures, `@elabs-ai/components-*` component facts) is [`references.md`](./references.md).
> Recommended waves:
> W1 `1.1` → W2 `1.2 ∥ 3.1 ∥ 7.1` → W3 `1.3 ∥ 2.1 ∥ 4.1` → W4 `2.2 ∥ 3.2 ∥ 5.1` →
> W5 `4.2 ∥ 5.2 ∥ 7.2` → W6 `4.3 ∥ 6.1`.

## Phase 1 — Command-aware graph core
- [x] WP 1.1 — contract v2: entry-point nodes, flows, keywords — done 2026-07-03 · wp/skill-ide/1.1 (21c39ec). `entry_point` node kind + additive `flowId`/`flows`; 10 new edit-op members (command/keyword/asset/file ops) with zod validation; quality/tool-diagnostic/trigger/publish shapes + constants (severity weights, engine versions, L1=500/L2=5000 ceilings — documented rationale). `validateEditOps` rejects unimplemented ops with a 400 naming the implementing WP (honest stubs). Backward-compat lock test (pre-IDE graphs parse unchanged). 19 new tests; gate green (487).
- [x] WP 1.2 — projector v2: /command + trigger detection, per-entry flows — done 2026-07-03 · wp/skill-ide/1.2 (375c833). `/heading` → entry_point (first token = command value; annotation `skillflow:command id=` pins ids); section subtree = the command's flow (flowId on every derived node/edge); frontmatter `keywords:` → keyword entry points on main; cross-flow references → edge + warning; flows[] always present. PROJECTOR_VERSION → 3. Regression lock proven against a frozen v2 snapshot (additive delta = flowId:'main' + flows[] only); existing projector tests pass unchanged. New multi-command fixture (11 nodes / 9 edges / 3 flows). 8 tests.
- [x] WP 1.3 — canvas v2: flow lanes, entry-point start nodes, flow picker — done 2026-07-05 · wp/skill-ide/1.3 (e959c47). Extended (not replaced) the hand-rolled `graph-layout.ts`: per-`flowId` lane bands (`main` first, then `flows` doc order, running `laneBaseY`), `entry_point` heads its lane; a single-`main`-flow graph lays out byte-identically (determinism preserved). Flow-picker `Select` (multi-flow only); a cross-flow edge renders only when both endpoints visible; Trace overlay filtered by the same predicate (no stranded verdict badges). Lane labels are render-only `@elabs-ai/components-flow` custom nodes so they track pan/zoom (sound deviation from the "screen-space div" suggestion; NOT a new `SkillGraphNodeKind`). Gate green on merged main (typecheck · test 727 · build · lint). **NOT verified (owner-acceptance):** both-theme / keyboard / on-screen appearance — the subagent could not launch the app or run Playwright (local browser-cache revision mismatch; no `playwright install`); reasoned from code + `.d.ts`, not observed.

## Phase 2 — Flow editor v2
- [x] WP 2.1 — edit-ops v2: add/rename/delete_command, set_keywords, connect/disconnect_asset — done 2026-07-05 · wp/skill-ide/2.1 (ed7c347). Six live 400-stubs replaced with real anchored-splice appliers (`roundtrip.ts`) + validation (`edit-ops.ts`): `connect_asset` aliases `add_asset_ref` (shared `applyAssetRef`, no dup logic); `add_command` reuses an extracted `insertSectionBlock`; `delete_command` cuts the whole flow subtree (annotation line → last-node `endLine`), leaving cross-flow "see /x" text byte-exact; `set_keywords` splices only the frontmatter `keywords:` block via `yaml`. Batch validation: duplicate `/command` token → 400, `delete_command` + in-flow op → 400, double `set_keywords` → 400. 19 new tests (716 total); byte-exactness + additive-delta regression locks proven (zero-annotation, github-style). Fix-forward: the obsolete WP 1.1 stub-guard in `skill-ide-contract.test.ts` (asserted "WP 2.1 not implemented") replaced with a real semantic check — verified not a coverage loss. Gate green on merged main. **Follow-up (projector gap):** a `see /deleted` ref left dangling after `delete_command` emits NO projector warning (`projector.ts` only warns on refs to *existing* commands) — deferred to a projector-touching WP (8.1/9.x).
- [x] WP 2.2 — canvas editing: command CRUD on canvas + drag-to-connect assets — done 2026-07-05 · wp/skill-ide/2.2 (ff1195e). Toolbar "Add command" → `CommandDialog` (token `/^\/[a-z0-9][a-z0-9-]*$/i` + dup warning); entry-point `NodeDetailPanel` gains `EntryPointCommandEditor` (rename via dialog, delete via destructive confirm listing the flow's sections); section→asset drag-connect (React Flow `onConnect`, edit-mode only, `isSectionNode(src) && target.kind==='asset'` guard else inline toast) → `connect_asset`; edge-delete (Delete/Backspace, keyboard-reachable) → `disconnect_asset`. All stage through `useEditOps`→`applyPreviewOps`→`SaveVersionDialog` (I2 — nothing mutates until Save; API 400s surface there); `delete_command` pre-discards conflicting staged ops on the entry node; `use-edit-ops.ts` "NEVER canvas drag-to-draw" comment corrected. Gate green on merged main (typecheck · test 750 · build · lint). **Live-verified** on a throwaway :8091 build with the multi-command fixture in **both themes** (add-command loop + rename/delete panel, no console errors). **NOT verified:** the React-Flow drag-drop gesture + full save→re-projection round trip (drag not scriptable; the save engine is WP 2.1, tested server-side). Deviations: `afterFlowId` picker omitted (append at doc end); keyword-trigger editing deferred to WP 6.1.

## Phase 3 — Workspace (files & folders)
- [x] WP 3.1 — tree edit ops: create/rename/move/delete files + folders → new version — done 2026-07-03 · wp/skill-ide/3.1 (3d61690). Pure `tree-ops.ts` (add utf8/base64, update, rename=move preserving blob sha, delete; SKILL.md rename/delete → 400; traversal/collision guards; caps over final tree); edit-ops file-op validation replaces the stub (unknown paths list valid ones; conflicting pairs → 400); edits route accepts mixed text+tree batches (text first, ONE createVersion; text-op + update_file(SKILL.md) → 400 ambiguous-writers). Diff labels renames via blob-sha identity (asserted). 17 tests. Note: route uses DEFAULT_INGEST_CAPS like the blank branch — env-cap pass-through is a 1-line index.ts follow-up.
- [x] WP 3.2 — Files tab → file manager (folder tree CRUD, Monaco editing, save-as-version) — done 2026-07-05 · wp/skill-ide/3.2 (957de37). `@elabs-ai/components-ui` `Tree` workspace under `skills/workspace/*` (`workspace-model` pure derive-ops · `use-workspace` staging · `WorkspaceTree`+selection toolbar · `WorkspaceEditor` Monaco/binary-view-only · dialogs · `SaveWorkspaceDialog`): folder/file create · rename · move(=`rename_file` batch) · delete · upload · edit → tree ops → ONE new version via the existing edits route; SKILL.md rename/delete affordances absent; binary view-only (server `isBinary`); `beforeunload`+Discard guards. **Closes the 3.1 env-cap follow-up:** `registerSkillflowRoutes(…, caps?)` + `index.ts` threads `skillCaps` + edits route uses the passed caps + a proving test (low env cap → 400 per-file/count; default stays permissive). Own staging (design/ untouched — Phase 9 unifies, sanctioned). +3 API tests (750 total). Gate green on merged main. **NOT verified:** no live browser walk of tree CRUD / both themes (Playwright browser-cache mismatch) — semantic-token-only, reasoned not observed. **Follow-up (parent integration):** after a Files-tab save the explorer re-bases its own file list but cannot drive `SkillInspector`'s version picker (out of surface) → the picker label stays stale until reload; close when `SkillInspector.tsx` is next edited (W6/4.3) or in Phase-3 acceptance prep. Per-file token badges dropped (interactive `Tree` label doesn't right-align accessories).

## Phase 4 — Quality
- [x] WP 4.1 — quality engine: deterministic rules + score + route — done 2026-07-05 · wp/skill-ide/4.1 (c206b33). Pure versioned `quality.ts` (10 rules: manifest-incomplete, trigger-hygiene, l1/l2-budget, broken-ref, unused-asset, script-undocumented, gatekeeper-no-breadcrumb, orphan-section, command-collision-internal); score `clamp(100−Σweights,0,100)` consuming shared weights; `GET /versions/:vid/quality`; env ceilings `SKILL_QUALITY_L1/L2_TOKEN_CEILING`. Reuses `suggestions.ts` breadcrumb ops via a new additive export `buildMissingBreadcrumbOps` (import-not-copy, behavior-preserving). **Rule↔guide contract enforced:** `docs/skill-authoring.md` gains `command-collision-internal` + 9 rules flipped planned→enforced; a test parses guide headings and fails if any emitted ruleId lacks an anchor. `fix` ops shape-validated via `validateEditOps`, never applied (no 2.1 dep). 11 tests (727 total); determinism + route≡engine locked; messy fixture→53, zero-annotation→95. **Sound deviations:** `broken-ref` excludes two design-legitimate projector-warning categories (else clean fixtures false-error); `guideAnchor` is derived via `qualityGuideAnchor(ruleId)`, not a stored field (frozen `QualityFinding`, no shared reshape) — WP 4.3 consumes the helper. Gate green on merged main. **NOT verified:** no live/UI (API-only; Quality tab is WP 4.3).
- [x] WP 4.2 — static optimization suggestions (trace-less rules in the suggestion engine) — done 2026-07-05 · wp/skill-ide/4.2 (f67713e). `buildStaticSuggestions(graph, files, footprint)` in `suggestions.ts`; `GET …/suggestions` WITHOUT `runId` → static (runId keeps trace-based). Rules: `split-oversized-body` (L2>ceiling → largest section → REAL `add_file reference/<slug>.md` + `update_section_body`, collision-safe slug), `dedupe-keywords` (REAL `set_keywords`, case-insensitive), `remove-unused-asset` + `tighten-description` (advisory). No-corruption guard `finalizeStaticOps` (invalid ops → advisory, never emitted broken); cross-fixture invariant (all 6 fixtures, ceilings=1: never throws, deterministic, every non-empty-ops suggestion passes `validateEditOps`). +10 tests (759 total). Gate green on merged main. **Deviation (accepted + handoff):** could not extend the trace `SkillSuggestion` union — web `SuggestionCard`'s label map is a **total** `Record<SkillSuggestionRule,…>` (off-surface), so a separate additive vocabulary `SkillStaticSuggestion` / `SKILLFLOW_STATIC_SUGGESTION_RULES` (sharing the `SkillEditOp[]` fix-op vocab) was added, surfaced via additive `SkillSuggestionsResponse.staticSuggestions?`. **→ WP 4.3 MUST render `staticSuggestions` (extend `SuggestionCard` with a static label map).**
- [x] WP 4.3 — Quality tab: score card, findings, apply-able fixes — done 2026-07-05 · wp/skill-ide/4.3 (9efdad8). `quality/QualityView.tsx` + children (`QualityScoreCard`, `FindingCard`, `ToolDiagnosticsSection`, `quality-meta.ts`); Quality tab after Trace; score card + severity breakdown; findings (severity badge, ruleId chip, message, "Why?" link derived client-side `docs/skill-authoring.md#<ruleId>`); apply-fix (finding `fix` / static-suggestion ops → `SaveVersionDialog` → `postSkillEdits` → new version → `handleDesignSaved` refetch → score re-runs; 409 re-fetch); static suggestions via a discriminated `variant` union on `SuggestionCard` (default `"trace"` so the off-surface caller compiles unchanged) reading `staticSuggestions` (**the 4.2 handoff**); tool-diagnostics section reusing `getToolDiagnostics`; Re-run checks. **Closes the WP 3.2 follow-up** — `SkillFileExplorer` gained `onVersionSaved` → the inspector refetches so the version picker no longer stays stale. Both themes screenshotted (score card, findings, empty states). Gate green on merged main (759). **NOT verified (owner-acceptance / richer fixtures):** populated apply-fix score-improves walk, populated static-suggestion + tool-diagnostic lists, the anchor "View in Design" jump (best-effort tab switch; true cross-tab node selection needs a `SkillDesignView` edit, out of surface).

## Phase 5 — MCP-aware smart validation
- [x] WP 5.1 — tool-reference extraction + validation vs latest MCP scans (diagnostics) — done 2026-07-05 · wp/skill-ide/5.1 (ffda26f). Pure `tool-validation.ts` (imports only shared + `compare/matching`): conservative extraction (backticked `snake`/`kebab`/`server:tool` shape AND a context word tool/call/invoke/use on the same/adjacent line; frontmatter + fenced code skipped; bare words never match), exact→normalized→fuzzy(≥0.6) match vs each scoped server's LATEST **successful** scan; `unknown_tool`/`stale_tool` + ≤3 candidates; `unscannedServers` for zero-scan servers (no false unknowns). `servers` annotation registered (no longer warns); `GET …/tool-diagnostics`; `TOOL_VALIDATION_VERSION` stamp; additive shared `ToolDiagnosticsReport.unscannedServers?`. 20 tests (747 total); **never-scan invariant proven by static import scan** (no MCP client / openSession / tool-bridge / child_process / ScanRepository in the module). Gate green on merged main. **Sound deviations:** the projector-tool-call-name extraction branch is inert (projector v3 emits none) → context-signal is the sole live path; uses `listSummariesByServer().filter(success)` not `getLatestForServer` (status-aware); the route reaches the canonical `AppDatabase` via a documented cast off `SkillRepository` + a secret-free `SELECT id,name FROM mcp_servers` to avoid touching the frozen `registerSkillflowRoutes` signature (3.2's this wave). **Minor follow-up:** move that inline server-list SQL into a repository method later (architecture-layering polish).
- [x] WP 5.2 — editor markers + canvas tool badges + validation-scope config — done 2026-07-05 · wp/skill-ide/5.2 (1effa84). Monaco warning markers (single owner `tool-validation`, cleared on re-validate AND unmount — dispose invariant proven) in `NodeDetailPanel`'s body editor + `WorkspaceEditor` (SKILL.md-only guard); canvas warning `Badge` on section nodes via pure `diagnosticsInRange` (anchor-span match), separate from the trace visit badge; per-node `NodeDiagnosticsList`; `ValidationScopeEditor`; `getToolDiagnostics` + `formatToolDiagnosticMessage` client. Gate green on merged main (759). **Badge wiring VERIFIED complete by orchestrator** — the agent's self-reported "needs a `SkillDesignView` wire" was a false alarm: `SkillDesignView` already passes `skillId`/`versionId` to `SkillGraphCanvas` (lines 478/515, from 2.2's save context) and the self-fetch consumes them. **NOT verified:** live both-theme render of markers/badges (Playwright pin mismatch) — owner-deferred (Phase 5). **Deviation (sound):** scope editor stages `update_section_body` writing the `skillflow:servers` comment into the first section's body (frozen `set_annotation` can't carry a doc-scope list; the validator parses it globally). **Follow-ups:** a cleaner doc-scope annotation op later; a section-less skill can't set scope via the editor.

## Phase 6 — Keywords & triggers
- [x] WP 6.1 — trigger-surface manager + cross-skill collision report — done 2026-07-05 · wp/skill-ide/6.1 (0a098b9). API `triggers.ts` (`getTriggerSurface` = description + frontmatter keywords + command entry points from the projected graph; `getTriggerCollisions` = every skill's CURRENT version grouped by `(kind, stemPlural(normalizeName(value)))` — imported `normalizeName` + a **documented conservative `stemPlural`** [sibilant+es, regular trailing-s, `ss` intact, irregulars unfolded]; a value claimed by ≥2 distinct skills → ONE collision with id-sorted `skillIds`; command=error, keyword=warning); `GET …/triggers` + `GET /api/skills/trigger-collisions`. Web: Overview Triggers panel (`TagInput` → `set_keywords` via `postSkillEdits` → new version → `onVersionSaved`; commands list → `onOpenFlow` Design switch) + `SkillsView` registry collisions section (severity badges, deep-links, clean empty state). 10 tests (769 total); load-bearing fixture proven (two skills sharing `/report` + `analytics` → exactly one command + one keyword collision, each `[A,B]`). Both themes screenshotted; live `/trigger-collisions` returned the 2 expected. Gate green on merged main. **NOT verified:** cross-component live refresh (keyword edit → collisions panel uses its Refresh button; no shared store, house style); command deep-link pre-selects only the Design tab, not the specific flow (`SkillDesignView` off-limits — `onOpenFlow` carries the flowId for future wiring).

## Phase 7 — Publish to GitHub
- [x] WP 7.1 — API: create GitHub repo from a version + initial push + bind as source — done 2026-07-03 · wp/skill-ide/7.1 (ab3796d). `SkillPublishService` w/ injectable `createRepo` (REST POST /user/repos default; tests inject file:// bare repos — fully offline); credential discipline extracted from git-service into shared helpers (one implementation: argv-only helper, SSRF DNS guard, redactUrl); refusal matrix (404s, already-bound 409, no-token 400, existing/non-empty remote 409 via ls-remote probe + non-FF push → 409, NO force-push; REST errors mapped 409/401/502 redacted); bind sets github fields + encrypted PAT with last_sha = pushed HEAD (immediate pull → unchanged, verified). Token proven absent from responses AND captured logs. New `bindGithubSource` repo write (justified — update() can't change source). 7 offline tests. ⚠ live REST path needs a real PAT (owner acceptance).
- [x] WP 7.2 — UI: Publish-to-GitHub wizard — done 2026-07-05 · wp/skill-ide/7.2 (110527c). `PublishGithubDialog` (name via shared `GITHUB_REPO_NAME_PATTERN` + `.`/`..` guard; private + bind toggles; PAT `type=password`, never prefilled, `autocomplete=off`, `spellCheck=off`, sent only in the POST body, never echoed/logged; encrypted-note shown ONLY when bind-as-source on); `SkillInspector` version-scoped "Publish to GitHub" action; submit enabled until the request starts then `Spinner`; 409→warning / 4xx→destructive `Alert` with the server's redacted message verbatim (no client refusal logic); success view links out + refetches skill+versions (GitHub badge/pull appear). `publishSkillToGithub` client (own import block; the EOF append conflict with 5.2 was orchestrator-resolved keeping both). Gate green on merged main. **Both-theme form screenshotted** (Playwright via `PLAYWRIGHT_CHROMIUM_PATH`). **Deviation (sound):** `alreadyBound` disables the rebind toggle (API 409s a silent rebind). **NOT verified (owner-only):** live PAT publish (real repo create+push+bind); success/error-banner views not screenshotted.

## Phase 8 — Server-bound skill authoring (I9, owner-locked 2026-07-04 · waves W7–W9, AFTER W3–W6)
- [x] WP 8.1 — contract + binding (frontmatter `servers:` → exact registered server) + `tool_ref` projection — done 2026-07-05 · wp/skill-ide/8.1 (d57a2db). Additive shared: `tool_ref` node kind `{toolName, serverName?}`, `add_tool_ref` op (400-stub → 8.3), `SkillServerBinding`/`SkillBindingsInput`, `SkillManifest.servers?`, `SKILLFLOW_PROJECTOR_VERSION` 3→4. **Migration v17** (`skill_server_bindings`, PK `(skill_id, server_name)`, additive `CREATE TABLE IF NOT EXISTS` — the v13/v15 pattern, NOT the v16 rebuild) — **forward-safe proven** (fresh DB stamps 17 + carries the table; a v16 DB gains it on `applyMigrations`; all prior v13–v16 migration tests still pass). Single `extract-tools.ts` (5.1 heuristic lifted; `tool-validation.ts` refactored to re-export+consume — **5.1's tests pass unchanged; same function object asserted**). Projector emits `tool_ref` accessory leaf + `calls` edge from TEXT EVIDENCE only (purity/never-scan proven by static import scan; **zero-reference regression lock** — all 6 fixtures project zero `tool_ref`, an extended-in-memory fixture projects 2). `GET`/`PUT /api/skills/:id/bindings` (names auto-resolved to registered ids; unmatched/ambiguous → `serverId:null`, never guessed; unknown serverId→400, server-delete SET-NULL, skill-delete cascade, unknown skill→404). Frontmatter `servers:` byte-exact (read-only parse). `node-kind-meta` wrench glyph (muted). 788 API tests (+19). Gate green on merged main. **Required fix-forwards (contract bump):** `skillflow-contract.test.ts` (PROJECTOR_VERSION 3→4 + `tool_ref` in the exhaustive node-kind fixture), `benchmarks-{contract,suites-contract,collections-contract}.test.ts` (`LATEST_SCHEMA_VERSION` 16→17). **NOT verified:** `tool_ref` glyph not rendered live in either theme (owner-deferred, Phase 8).
- [x] WP 8.2 — editor assistance: completion + hover popup from bound servers' latest scans — done 2026-07-05 · wp/skill-ide/8.2 (a883ac6). `ScanRepository` DI into `registerSkillRoutes` (positional after `servers`; test callers omit → route guards `if (!scans) return []`; `index.ts` + the new test supply it; API tests aren't typechecked so no caller churn). `GET …/bound-tools`: resolves the skill's bindings (reuses 8.1's resolver, `serverId!=null`) → each server's STATUS-AWARE latest success scan (`listSummariesByServer().find(success)` — NOT `getLatestForServer`, source-grep-locked) → `BoundTool { serverName, toolName, description, schemaParams:[{name,type,required}], definitionTokens }` (schemaParams from `input_schema_json` top-level properties + required; tokens = `total_tokens`). Additive shared `BoundTool`/`BoundToolParam`. Web `use-bound-tools.ts` + `registerBoundToolProviders`: backtick-context completion (detail = owning server) + hover popup (name+server, description, params, tokens) in `NodeDetailPanel` body editor + `WorkspaceEditor` (SKILL.md); **model-scoped** (no bleed into read-only/diff editors), registered once per mount + **disposed on unmount/tool-set change**. Unbound → no providers (honest degradation, tested). "Test this tool…" gated on an optional `onTestTool` prop (hidden until WP 8.5 injects it — no dead control). Never-scan proven (seeded scan returned verbatim). 12 tests (800 total); completion widget screenshotted both themes. Gate green on merged main. **NOT verified:** the hover POPUP visual (only completion screenshotted — Monaco hover fragile to script); disposal is code-proven (no web test infra); the `onTestTool` branch is dormant until 8.5.
- [x] WP 8.3 — tools palette + drag-to-reference + tool detail card + footprint readout — done 2026-07-05 · wp/skill-ide/8.3 (d49ef1d). `add_tool_ref` implemented (edit-ops `requireSectionNode` like `add_asset_ref`; roundtrip `applyToolRef` reuses the SAME `appendToBodyEnd` primitive `applyAssetRef` delegates to — no duplicated splice; byte-exact pure body-append proven; default `` Call `<tool>`. `` re-projects as a `tool_ref` + `calls` edge [unit + route level]; accessory-node target → 400). Web `ToolsPalette` (per-server groups from `use-bound-tools`, `SearchInput` filter, per-tool token cost, header footprint = Σ definitionTokens of tools **referenced ∩ resolved**, scan-derived); drag-onto-section (`application/x-mcp-tool` dataTransfer → canvas `onDrop` hit-test → validate section-ness vs the raw graph → stage `add_tool_ref`, preview before Save, I2) + insert-at-cursor (Monaco `executeEdits` → `update_section_body`); `tool_ref` click → tool card **Resolved** (server/desc/params/tokens + `/servers/:serverId` deep link + 8.5-gated Test run) / **Unbound**. 5 tests (814 total). Gate green on merged main. **Sound deviation:** no binding-picker UI exists (bindings resolve from frontmatter `servers:`) → the unbound empty-state is actionable text guidance, not a dead link. **NOT verified:** the resolved-card + non-zero footprint were not screenshotted (the `echo` fixture's bare-word tool name can't project as a `tool_ref` — a snake_case-tool server is needed for the Phase 8 owner walk); palette + Unbound card screenshotted both themes; `onTestTool` dormant until 8.5.
- [x] WP 8.4 — scaffold: new skill from a server — done 2026-07-05 · wp/skill-ide/8.4 (cf1a0b3). `POST /api/skills/scaffold-from-server` (frontmatter `name`/description-stub/`servers:[<name>]` + intro + one `## <tool>` section per selected tool = the scan description's FIRST SENTENCE + a ``Call the `<tool>` tool.`` line [backtick ref + the context word `extract-tools.ts` needs] → **exactly one `tool_ref` per tool**, projector-asserted); shared `persistScaffoldedSkill` helper (blank + server paths share it, no signature change); creates v1 + `skill_server_bindings` rows resolved to the source server; status-aware latest-success scan; servers-without-scan → 400, unknown tool → 400, unknown server → 404, empty set → 400, slug collision → 409; never-scan proven. Additive shared `ScaffoldFromServerInput`/`Result`. Web `ScaffoldFromServerWizard` (server picker [only `success`-scan servers] → `@elabs-ai/components-data` `DataTable` tool multi-select w/ token costs → name/slug) on `SkillsView`. 9 tests (809 total); both themes screenshotted. Gate green on merged main. **Follow-up (minor UX):** "open in Design tab" degrades to a hard-nav landing on Overview (`SkillInspector` tab state + `App.tsx` routing out of surface; Design one click away) — fold a tab deep-link into Phase 9's inspector rework.
- [x] WP 8.5 — inline tool test run from hover/tool card — done 2026-07-05 · wp/skill-ide/8.5 (9d5b5fb). `ToolRunner` extracted VERBATIM from `ToolPlayground` (form + `tools/call` + Monaco result + 4 token/byte KPIs + OAuth-reauth retry) — the scans `ToolRunDialog` now composes it with `confirmDestructive` default off, so the playground path is functionally identical (typecheck + build + the unchanged tool-call route are the regression lock). `ToolRunnerSheet` (`@elabs-ai/components-*` `Sheet`, binding-resolved, schema+annotations from `GET /api/servers/:id/latest-scan`); hosted in `SkillInspector`, `onTestTool` threaded to ALL THREE affordances — Design tool card (resolved→enabled / unresolved→**disabled with reason**), Design body-editor hover, Files SKILL.md hover (none dormant). **Strictly user-initiated** (nothing on hover/open; `tools/call` only on Run/confirm), **destructive/open-world `AlertDialog` confirm** naming the flags, **ephemeral** results, reuses the existing endpoint (no new endpoint; never-execute-skill invariant untouched — running a TOOL ≠ running skill content). 830 tests (web-only). Gate green on merged main. **Sound deviation:** resolves full schema/annotations via `GET latest-scan` rather than enriching `BoundTool` (avoids an out-of-surface API edit). **NOT verified (owner-acceptance):** the live resolved-Sheet → form → run → result walk (needs a running stub + a snake_case-tool fixture; `echo`'s bare-word name can't project a `tool_ref` → no enabled Test button deterministically) + the destructive-confirm against a real annotated fixture; only the unbound disabled affordance screenshotted both themes.

## Phase 9 — Unified Flow/Code editing + education layer (I10, owner-locked 2026-07-04 · waves W10–W12)
- [x] WP 9.1 — live-draft engine: apply-preview + project-preview (stateless), save w/ intent log, staging migration — done 2026-07-05 · wp/skill-ide/9.1 (507621b). **Single implementation** `applyOpsToContent(content, files, ops, graph?)` (= `applyEditOps` over `projectSkillGraph`) shared by the persisted edits route AND `POST /api/skillflow/apply-preview` → **apply-preview ≡ persisted splice byte-for-byte** (asserted every fixture: preview `content'` === `applyOpsToContent` === the SKILL.md the real edits route writes); `POST /api/skillflow/project-preview` ≡ `GET …/graph` deepEqual (all fixtures, `SKILLFLOW_PROJECTOR_VERSION`-stamped); both endpoints pure (persist nothing, no scan/MCP). Anchor-fail → 400-with-reason. **Migration v18** (additive `skill_versions.intent_log_json`, `ensureColumn` — the v17 pattern) — forward-safe proven (fresh→18 has the column; a v17 DB gains it + preserves rows). `POST /api/skills/:id/save-draft {baseVersionId, content, treeOps, intentLog, note?}` → ONE version via shared `createVersion` (SKILL.md===draft, `intent_log_json`===intent log verbatim, 409 on moved head, `{unchanged:true}` on identical). Web `use-skill-draft.ts` (content + tree ops + intent log + debounced project-preview w/ last-good retention; `save()` recomputes fresh to defeat the debounce race). **Mechanical migration** — `useEditOps` public API unchanged (`NodeDetailPanel` needed NO edits; canvas keeps `applyPreviewOps` so 2.2 command CRUD is intact); `SaveVersionDialog.save` optional with a `postSkillEdits` fallback so the Quality/Trace apply-op flows keep working. 16 tests (830 total). Gate green on merged main. **Required fix-forwards:** `benchmarks-{contract,suites-contract,collections-contract}.test.ts` + `skill-ide-server-binding.test.ts` `LATEST_SCHEMA_VERSION` 17→18 (version literals only). **NOT verified:** web not run live (typecheck+build+lint only) — the visible unified Flow/Code surface is WP 9.2; the 409 "Reload" re-fetches the same immutable base (full version-switch UX is 9.2).
- [x] WP 9.2 — unified editor shell: Show flow | Show code | Split, anchor selection sync, one Save bar — done 2026-07-05 · wp/skill-ide/9.2 (0f6cefd). `UnifiedEditor.tsx` — `@elabs-ai/components-ui` `ToggleGroup variant="segmented"` (Flow | Code | Split); Flow = the existing canvas, Code = a full-document `CodeEditor` bound to `draft.content`, Split = `ResizablePanelGroup` (autoSaveId, withHandle) — the code `ResizablePanel key="code"` is STABLE across Code↔Split so Monaco's model is never remounted. **One source of truth** (I10): `useSkillDraft` (9.1) is the only state — canvas gesture → `addOp` → apply-preview → `draft.content` → (Split, live) the Code pane; code edit → `setContent` (new additive `manualEdit`, clears ops) → debounced project-preview → `draftGraph` → the canvas re-projects; neither view owns state so they can't diverge. **Anchor selection sync both ways** (node click → reveal `anchor.startLine`; cursor move [debounced 120ms] → select the innermost owning-anchor node) with a **two-way loop guard** (`selectionSourceRef` + owner-equality). **One Save/Discard bar** — own `UnifiedSaveDialog` drives `draft.save` for text edits AND staged ops (listed together; 409/400/unchanged/success; deferred version-switch; View-full-diff); Discard → `draft.reset`. Deep links `?mode`/`?node`/`?line` via `useSearchParams` (no new App.tsx routes; stripped after apply — ready for 9.4). `SkillDesignView` → thin `<UnifiedEditor defaultMode="flow">`; the Files-tab SKILL.md entry opens `<UnifiedEditor defaultMode="code">`. Both themes screenshotted (Flow/Code/Split). 830 tests (web-only). Gate green on merged main. **Sound deviations:** own `UnifiedSaveDialog` (the existing `SaveVersionDialog` hard-disables on `ops.length===0`, out of surface); the unified surface is always-editable (old Design "Edit mode" toggle removed). **NOT verified (owner-acceptance):** the live flow↔code round trip + split selection sync + no-double-mount are by-construction/code-review (screenshots confirm rendering, not the driven interactions). **Follow-up:** the Files-tab SKILL.md draft store is independent of the workspace-tree store — interleaving a SKILL.md draft edit with pending tree edits in one session is an out-of-scope edge case.
- [x] WP 9.3 — code-mode intelligence: kind decorations, construct hovers, authoring snippets — done 2026-07-05 · wp/skill-ide/9.3 (8653ed5). `design/code-intel/` (monaco-types, explainers, snippet-specs, decorations, hovers, snippets, index, decorations.css) registered in `UnifiedEditor`'s `CodePane` `onMount`, disposed on unmount; fed the live projection + bound-tools + file-paths via `setGraph/setBoundTools/setFilePaths`. **Decorations from the live `project-preview` graph** (token-colored, no raw literals): kind gutter glyph per heading, flow-extent whole-line rail per `/command` (`--chart-1..5`), annotation + breadcrumb markers/underlines, asset + tool-ref dashed underlines. **Construct hovers** (frontmatter keys, `skillflow:*`, breadcrumb markers, headings→kind, asset/tool refs) → local explainer + `docs/skill-authoring.md#…` anchor; **tool-ref hover defers to WP 8.2** (bound → `null`, no double hover; unbound → generic explainer). **9 snippets** + dynamic relative-path asset completion — each asserted through the REAL `POST /api/skillflow/project-preview` → intended node kind (`snippet-specs.ts` imported verbatim by the API test → no drift). **Disposal proven** (register once/one `dispose()`; `setBoundTools` disposes the prior; stable `CodePane key` so Monaco never remounts). Local `explainers.ts` (`EXPLAINERS` map + single `explainerFor()` lookup; every `guideAnchor` a real `docs/skill-authoring.md` heading) — structured for WP 9.4 to swap for the shared registry. 15 API tests (845 total). Gate green on merged main. **NOT verified:** the committed-e2e hover screenshot didn't frame the popup (line below the 1280×720 fold — non-gating); hover render + full decoration set proven by out-of-band captures; a chromium harness quirk left dark unrepainted out-of-band (the committed e2e paints dark correctly).
- [x] WP 9.4 — education layer: explainer registry (guide-anchored) + unified problems panel — done 2026-07-05 · wp/skill-ide/9.4 (f33541c). **Single-source `EXPLAINERS` registry** (`code-intel/explainers.ts` expanded): 6 node kinds + `tool_ref` + edge kinds (`edge:sequence/branch/reference`) + frontmatter keys + annotation keywords + breadcrumb marker + trigger + asset/tool refs, each `{id,title,short,guideAnchor}`. WP 9.3 consumes it UNCHANGED (`hovers`/`decorations`/`snippets` via `explainerFor`). **Guide-anchor test** (parses `docs/skill-authoring.md` headings via GitHub slug rules → every `guideAnchor` resolves; bogus rejected) + **single-source test** (every snippet `explainerId`, node kind, hover id ∈ registry). **Unified `ProblemsPanel`** (mounted once in `UnifiedEditor`, identical in Flow/Code/Split): pure `collectSkillProblems` aggregates **live projector warnings** (`syncGraph.warnings`) + persisted **quality findings** (`getQualityReport`) + persisted **tool diagnostics** (`getToolDiagnostics`); each row a **triple deep-link** — node (`onGoToNode` → select + switch to a flow-visible mode), line (`onGoToLine` → reveal via 9.2's pending-reveal), guide anchor (registry); **live-vs-persisted surfaced honestly** ("Flow warnings are live on your draft. Quality and tool checks are for the last-saved version"; quality/tool re-fetch on `versionId` change); educating empty state. `NodeDetailPanel` "What is a `<kind>`?" card; legend popover in the `UnifiedEditor` toolbar (WP-sanctioned placement). 6 API tests (851 total). Gate green on merged main. **Sound deviations:** legend in toolbar not a canvas overlay (React Flow stacking reliability); the standalone What-is-this canvas-node smoke screenshot dropped (headless node-click flaky — deterministically covered; same affordance in each panel row). Both themes screenshotted (panel in Flow + Code + legend).

## E2E validation (2026-07-05 · orchestrator-run, provider-free)
All 20 open WPs are merged into local `main` (`c9ab6f8` at write time) with the four-command gate
green on the final merge (**typecheck · test 851/851 · sequential build · Biome lint**). The
per-WP route tests already exercise every new endpoint end-to-end via `app.inject`; on top of that
the FINAL merged build was booted from a **fresh DB** on `:8099` and driven with real HTTP.

**✅ Verified LIVE (provider-free):**
- **Boot + migration** — the built `apps/api/dist` serves; a fresh DB migrated to **`user_version = 18`** at startup (health 200).
- **Projector v4 / live-draft previews (9.1, 1.2, 8.1)** — `POST /api/skillflow/project-preview` on a 2-command SKILL.md → `projectorVersion 4`, entry points `/report`+`/analyze` (commands) + `report`+`analyze` (frontmatter keywords), per-command flows `main`/`report-daily`/`analyze`, and a `tool_ref` for the backticked snake_case `list_tables` (text-only). `POST /api/skillflow/apply-preview` with `add_command /summarize` → the new `## /summarize` heading present, the original `## /report daily` preserved byte-exact.
- **Content-canonical save (9.1)** — `POST /api/skills/:id/save-draft` created v2 from full content; the persisted `GET …/graph` of v2 matched project-preview (same kinds/flows/`projectorVersion 4`).
- **Quality (4.1)** — a well-formed skill scored **100** (`qualityEngineVersion 1`, zero findings). **Static suggestions (4.2)** — `[]` (nothing to optimize on the small skill).
- **Triggers + collisions (6.1)** — the surface returned keywords `[report, analyze]` + commands `[/report, /analyze]`; `GET /api/skills/trigger-collisions` → `0` (clean single-skill registry).
- **Tool-reference validation (5.1)** — `GET …/tool-diagnostics` flagged `list_tables` as **`unknown_tool`** (no registered server exposes it) — the "broken tool ref flagged" walk item, live.
- **Bindings (8.1)** — `GET …/bindings` → `[]` (no `servers:` frontmatter / none registered → honest unbound, never a guess).
- **Honest refusals** — `POST /scaffold-from-server` with a bad `serverId` → **404**; `POST …/publish-github` with no token → **400**.

**⏸ Needs a browser and/or a registered stub server (owner-acceptance — prepared, not run here):**
- The driven UI walk (canvas lanes + flow picker · add/rename command on canvas · drag-connect asset + Tools palette · tool test-run `Sheet` · Files workspace round trip · Quality-tab finding → guide link → apply-fix → score improves · canvas tool badges/markers · collision report UI · Publish wizard · unified **Flow | Code | Split** sync → one save → one version · problems-panel triple deep-links). Per-WP smoke screenshots exist in both themes; the end-to-end *driven* walk is owner-acceptance.
- **Live tool surface (8.2/8.3/8.4/8.5)** needs a registered MCP server whose tool names are **snake_case/kebab/namespaced** (the `echo` stub's bare-word `echo` cannot project a `tool_ref`) — e.g. a server exposing `list_tables`/`run_query`. Then: bound-tools completion/hover, the palette footprint, scaffold-from-server → live palette, and the **resolved** tool test-run walk (+ the destructive-annotation confirm on an annotated tool).
- **Live PAT publish (7.2)** and any **provider/LLM** path (not part of Skill IDE) remain owner-only.

## Owner acceptance (deferred visual / a11y — owner-only)
- [ ] Phase 1–2 — two-theme walk: a multi-command skill's flow lanes, command create/delete on
      canvas, drag-connect to an asset, save → clean SKILL.md diff — accepted: ____
- [ ] Phase 3 — folder/file management round trip incl. move + edit — accepted: ____
- [ ] Phase 4 — quality score + an applied fix — accepted: ____
- [ ] Phase 5 — a broken tool reference flagged against a real registered server's scan —
      accepted: ____
- [ ] Phase 6–7 — collision report; publish a skill to a fresh GitHub repo and pull it back —
      accepted: ____
- [ ] Phase 8 — bind a skill to a real registered server: completion + hover popup show real
      tools with token costs; drag a tool onto a section → `tool_ref` node + clean diff;
      scaffold a new skill from the server and see it project; **test-run a real tool straight
      from the hover popup and from a `tool_ref` node card** (destructive confirm fires where
      annotated; result + measured tokens shown) — accepted: ____
- [ ] Phase 9 — round trip in one sitting: edit on canvas → Show code (change is there) → edit
      text → Show flow (graph updated) → split view syncs selection both ways → one Save, one
      version; hovers/legend explain every element with working guide links; problems panel
      deep-links node + line — accepted: ____

## Decision log
_Owner-of-record: Claude (Skill IDE orchestrator). Each entry: date · decision · rationale. All
within the locked bounds of I1–I10 / SkillFlow D1–D8; none is a STOP item (§1 of the mission)._

- **2026-07-05 · WP 8.1 migration = `user_version = 17` (NOT v16).** The kickoff prompt (authored
  earlier) said "v13–v15 taken; take v16." Reality on `main@d6a262a`: the parallel **Testing-IA**
  workstream already claimed and merged **v16** (its WP 1.2, done 2026-07-05 — `database.ts`
  `MIGRATIONS` array ends at `version: 16`, `rebuildCollectionsForOptionalRepo` /
  `rebuildSuiteRunsForOptionalSuite`). Benchmarks holds v13–v15. Per "ledgers win over the prompt"
  + "take the next free," WP 8.1's `skill_server_bindings` migration claims **v17**; no sibling
  ledger's decision log claims v17. Re-verified at claim time in W7. _Rationale: never two
  migrations with the same number across parallel workstreams._
- **2026-07-05 · Orchestration = hand-driven named-branch worktrees.** One coding subagent per WP
  in its own worktree on `wp/skill-ide/<id>` cut from the pinned current-`main` SHA; the
  orchestrator validates the 4-command gate in-worktree, merges in dependency order with a full
  gate on `main` after each merge, and is the sole writer of this ledger. Replicates `/next-wp`
  discipline while retaining the product-owner duties the mission assigns (decision log,
  cross-workstream serialization, owner-walk prep, wave reports). _Rationale: named branches +
  deterministic merge order are required for cross-stream serialization and honest ledger
  discipline._
- **2026-07-05 · Concurrent-state reconciliation + merge protocol.** Both Benchmarks (WPs 1.1–5.1)
  and Testing-IA (all WPs) ledgers show DONE/merged; the sole live hazard is the owner's
  uncommitted working-tree doc edits (`CLAUDE.md`, `roadmap/testing-ia/*`,
  `roadmap/testing/ia-restructure-handover.md`) — left untouched. Every merge is preceded by
  `git fetch` + a `main`-movement check; worktrees fork from a committed SHA so dirty docs never
  leak; **no force-push; no origin push without owner sign-off.** _Rationale: reconciles on-`main`
  integration with the known concurrent-session hazard (memory: concurrent-autocommit-in-repo)._
- **2026-07-05 · On-`main` gate build runs sequentially under concurrent-session memory pressure.**
  The root `pnpm build` (`pnpm -r --sort`) parallelizes independent package builds; with the
  concurrent testing-IA session also building, the web build (Monaco + Milkdown + Mermaid) OOM'd
  (SIGTERM / exit 143) even with `NODE_OPTIONS=--max-old-space-size=3400`. Re-running
  `pnpm -r --workspace-concurrency=1 build` (fully sequential) is green. Adopting the sequential
  build for the on-`main` gate while parallel workstreams are active. _Rationale: an OOM SIGTERM is
  a machine-memory constraint, not a code defect — typecheck/test/lint were green and the
  sequential build passes byte-clean._
- **2026-07-05 · W8 serialized 8.2 → 8.4 (both need `ScanRepository` in `registerSkillRoutes`).**
  Both bound-tools (8.2) and scaffold-from-server (8.4) are skills-level routes needing the
  per-skill binding repo (already a `registerSkillRoutes` dep, WP 8.1) AND server scan data
  (`ScanRepository`), which `registerSkillRoutes` does NOT receive. Injecting `scans` is a
  single-owner signature + `index.ts` change; adding it unused would fail the unused-param lint.
  So WP 8.2 owns the `ScanRepository` DI (adds the param, uses it for bound-tools) and merges
  first; WP 8.4 then consumes the wired `scans` for scaffold. Deviates from the "8.2 ∥ 8.4" wave
  plan. _Rationale: proper dependency injection over spreading WP 5.1's DB-handle cast; a
  deterministic single writer on the shared signature._
- **2026-07-05 · W10 serialized 9.1 → 8.5 (pervasive design/ overlap).** WP 9.1's live-draft
  migration reworks the shared design surfaces (`SkillDesignView`, `NodeDetailPanel`,
  `use-edit-ops`, `SaveVersionDialog`) onto the draft store, and WP 8.5 wires the tool-runner
  `onTestTool`/Test-run callback into those SAME surfaces (`SkillDesignView` hosts the runner
  `Sheet`; the 8.3 tool card + 8.2 hover consume the callback). A pervasive rewrite (9.1) plus
  additive wiring (8.5) on one file is not a mechanical merge, so W10 runs 9.1 first (critical
  path — W11's 9.2/9.3 depend on it), then 8.5 wires into the migrated surfaces. Deviates from
  the "9.1 ∥ 8.5" wave plan. _Rationale: 9.1 is the foundational engine + on the critical path;
  8.5 is an independent leaf that slots cleanly AFTER the migration settles._
- **2026-07-05 · The Phase-9 tail runs serial 9.2 → 9.3 → 9.4 (not 9.2 ∥ 9.3).** WP 9.3's
  code-mode intelligence (decorations from the live projection, construct hovers, snippets)
  registers on the FULL-DOCUMENT `CodeEditor` that WP 9.2's `UnifiedEditor` creates — building it
  against a non-existent editor in a parallel worktree is a fragile, non-mechanical seam. And WP
  9.4's explainer registry (the single source) is consumed by 9.3's hovers while its problems
  panel renders in 9.2's shell. So the tail runs strictly serial in the plan's OWN order: 9.2
  (shell + Code editor) → 9.3 (code-intel on it; hovers use a local explainer map) → 9.4
  (formalizes the shared registry that 9.3 then consumes, adds the unified problems panel in 9.2's
  shell, asserts the single source). Deviates from "9.2 ∥ 9.3" only in serializing (order
  unchanged). _Rationale: each tail WP consumes the previous one's surface; a parallel seam here
  would be fragile and non-mechanical to merge._
- **2026-07-05 · WP 9.1 claims migration `user_version = 18`.** The I10 save path attaches the
  staged-op **intent log** as version metadata, but `skill_versions` has no structured-metadata
  column. WP 9.1 adds an additive `intent_log_json TEXT` column via **v18** (the SKILL_MAX-style
  additive `ensureColumn`/`CREATE`-shape pattern, like v17 — NOT a table rebuild). v13–v15
  Benchmarks, v16 Testing-IA, v17 Skill-IDE 8.1; v18 verified free (migrations end at 17, no
  sibling ledger claims v18). _Rationale: never two migrations with the same number across
  workstreams; structured audit persistence needs its own column._
