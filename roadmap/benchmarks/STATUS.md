# Benchmarks — work-package status ledger · **PRIORITY: HIGH (parallel with Skill IDE)**

Living state for the **Benchmarks** plan, read and updated by `/next-wp benchmarks`. A box is
ticked **only** when that WP's Acceptance is met and the gate
(`pnpm typecheck && pnpm test && pnpm build && pnpm lint`) is green.

**Legend:** `[ ]` open · `[x]` done. Done lines record date + branch:
`… — done <YYYY-MM-DD> · wp/benchmarks/<id>`.

> Decisions locked in [`00-architecture.md`](./00-architecture.md) (B1–B15; Testing conventions
> + SkillFlow D1–D8 inherited). WP specs live in the `phase-*.md` files. Recommended waves:
> W1 `1.1` → W2 `1.2 ∥ 3.1 ∥ 4.1` → W3 `1.3 ∥ 2.1 ∥ 3.2 ∥ 4.2` → W4 `1.4 ∥ 2.2 ∥ 2.3 ∥ 3.3 ∥ 4.4` →
> W5 `3.4 ∥ 4.3` → W6 `3.5 ∥ 5.1`. (`packages/shared` / `db/schema.ts` / `run-service` writers
> serialize across waves AND across the parallel Skill IDE workstream.)

## Phase 1 — Grading foundation
- [x] WP 1.1 — contract: `expectations` + test metadata + `Grade` shapes + judge settings — done 2026-07-04 · wp/benchmarks/1.1 (aba5663). Front-loaded contract: all 6 `GraderId`s, `RunGrade`/`GradeStatus`/`GradeKind`/`JudgeMethod`, `TestExpectations`/`ReferenceLogic`, `JudgeSettings`, `ToolHygieneFinding` + zod; migration **v13** (`tests` += expectations_json/category/difficulty/tags_json; new `run_grades` append-only + generic `app_settings` KV); test-repo persist/hydrate. Gate green on merged main (typecheck · test **529/0**, +7 · build · lint). **Sound deviation (accepted):** `TestInput.tags` is optional on the *input* payload (`Omit<Test,…|"tags"> & {tags?}`) while `Test.tags` stays required — models the zod `.default([])` input/output split and avoids breaking web's `TestEditor` without touching web. Not verified: downstream consumption (by design — later WPs).
- [x] WP 1.2 — grader engine: `Grader` interface, `rouge1`, `value_match`, post-completion hook, `run_grades` — done 2026-07-04 · wp/benchmarks/1.2 (b26f751; merged 7308a87). `Grader` seam + registry + `finalAssistantText`; in-house `rouge1` (unigram-F1) + `value_match` (last-JSON-block, rel-tol 1e-6); append-only `GradeRepository`; `GradeService.gradeRun` (re-entrant, zero rows when no expectations); guarded auto-grade hook in `run-service.ts` (optional `grades?` ctor param, chained after the assertion hook); `GET /api/runs/:id/grades` → `{grades, latest}`. Gate green on merged main (typecheck · test **555/555**, +20 · build · lint). **Four invariants proven** + a static never-execute scan (no `openSession`/`tool-bridge`/`child_process` import in grading/). **Deviation (accepted):** for an expectations-bearing test, EVERY registered grader writes a row — a grader lacking its facet writes `unevaluable` (surfaced, never dropped), consistent with the honest-status rule; zero-row short-circuit only when the test has no expectations at all. `index.ts` route-registration conflict vs 3.1 resolved (both wired). Not verified: no live HTTP curl (service-level tested).
- [x] WP 1.3 — LLM judge (outcome): logprob-weighted G-Eval, judge settings API, re-grade — done 2026-07-04 · wp/benchmarks/1.3 (c7c4cda; merged 8fb1880). `outcome_judge` Grader (injectable `generate`/`resolveJudge` for offline tests): G-Eval `<rating>` rubric + logprob-weighted expected rating ported faithfully from `eval_utils.compute_insight_eval` (tag→first-number→`error`, never silent 0; `method` honestly `logprob_weighted`|`single_sample`); `AppSettingsRepository` (judge under key `'judge'`); `GET/PUT /api/grading/judge-settings` (unpriced model → 400) + `POST /api/runs/:id/grade` (re-grade, append-only). Judge cost via `estimateCost` → `judge_*` ledger. Gate green on merged main (typecheck · test **594/594**, +15 · build · lint). **Cost isolation proven** (`runs.cost_usd` byte-identical before/after; judge cost only on the grade row); logprob math hand-verified (7.25). Minimal sanctioned edits to `grader.ts` (optional `judge*` fields) + `grade-service.ts` `toInsert` (forward them — the only write path to `judge_*`). `index.ts` roster conflict vs 2.1 resolved (deterministic + tool_hygiene + outcome_judge). **NOT verified (owner/live):** the OpenAI logprob EXTRACTOR (`extractRatingLogprobs`) is untested against a real provider (AI SDK v7 hides logprobs; defensive read → `single_sample` fallback) — flagged for the Phase 1 live-judge acceptance walk.
- [x] WP 1.4 — UI: Grade panel (run console), grade chips (list + compare), judge settings, expectations editor — done 2026-07-04 · wp/benchmarks/1.4 (fcc54fc; merged 50c8807). Grade panel (`@elabs-ai/components-*` `MetricCard`/`StatusBadge`/`Collapsible`, per-grader score + method, judge reasoning, evidence deep-links reusing `onSelectStep`→`PacketInspector`, Re-grade), grade chips in runs list + compare, `DefaultJudgeCard` in Settings, Expectations section in `TestEditor`. Props confirmed via the brand-ui MCP. Gate green on merged main (typecheck · test **646/646** · build · lint). **Visual (both themes, isolated `:8099` instance, seeded):** Settings + judge card and the test-editor dialog render correctly; the GradePanel content is confirmed rendering (ROUGE-1 42%, Value match 100%, Tool hygiene **"Not evaluable"** = muted not red-0, Outcome judge 80% + reasoning, Trajectory judge 70%, Re-grade) — the panel + expectations section were **text-confirmed** (both scroll internally, so no single full-layout frame captured). **NOT verified (owner-acceptance — Phase 1 walk):** full a11y/keyboard walk; grades appearing on a LIVE run (needs a provider key). `unevaluable` visual-distinctness confirmed.

## Phase 2 — Process grading
- [x] WP 2.1 — `tool_hygiene` grader: scan-schema argument validation + misuse signals — done 2026-07-04 · wp/benchmarks/2.1 (14d0ee3, FF). Deterministic `ToolHygieneGrader` over the run's `tool_call`/`tool_result` steps + each server's latest scan (`getLatestForServer` — read-only, never `openSession`); in-house top-level JSON-Schema **subset** checker (`schema-check.ts`, no ajv): missing-required/wrong-type/unknown-property/enum-violation + trace signals tool-not-in-scan/tool-error-rate/identical-repeat/error-then-retry; documented weight table; score 0–1. Gate green on merged main (typecheck · test **579/579**, +15 · build · lint). **Never-execute proven** (static import scan); **no-scan/no-tool-calls → `unevaluable` (score null, never 0)**. **Sound deviation:** args-bearing `tool_call` step carries no `serverId` (it's on the correlated MCP-sink step, matched by `toolCallId` — engine F5); grader correlates via `toolCallId`, verified by a split-steps test. Note: auto-grade only fires when the test has an `expectations` block (B4 gate) — consistent with locked design. Not verified: no live run.
- [x] WP 2.2 — `trajectory_judge`: rubric port over `run_steps` vs `referenceLogic` — done 2026-07-04 · wp/benchmarks/2.2 (82dc830; merged 02eb95d). `trajectory_judge` (LLM, reuses the global default judge): operations-digest builder (paired `tool_call`/`tool_result` → size-bounded Operation blocks with disclosed truncation), rubric ported faithfully from `compare_tools_vs_code.py` (0–10 table, dimensions/measures/filters/groupings/sort, omissions/redundancy, fence/truncation-tolerant parse). **New additive `Grader.appliesTo?()` seam** (grader.ts + grade-service.ts filter) → trajectory judge SKIPS runs with no `referenceLogic` (no row, **no wasted judge call**). Gate green on merged main (typecheck · test **641/641**, +17 · build · lint). **Invariants proven:** skip→no-row+no-call; digest truncation disclosed; cost isolation (`runs.cost_usd` byte-identical); evidence = deterministic shown-op idxs that resolve to real steps. Minor: small timeout/ledger helpers duplicated locally (judge.ts read-only) — future DRY cleanup. `index.ts` roster conflict vs 2.3 resolved. Not verified: no live provider round-trip.
- [x] WP 2.3 — `skillflow_conformance` scored grader over existing alignment verdicts — done 2026-07-04 · wp/benchmarks/2.3 (de3a402, FF). Pure deterministic grader over the run's persisted `AssertionResult[]` (`ctx.run.assertionResults` — read-only, no re-alignment, no LLM): score = weighted (`{skillGate:0.5, skillRoute:0.3, noFractures:0.2}`) pass-fraction per facet, renormalized over facets actually present; persisted `unevaluable` verdicts excluded from denominators (never a fail). Gate green on merged main (typecheck · test **624/624**, +12 · build · lint). **Invariants proven:** no-skill/no-verdicts/all-unevaluable → `unevaluable` (score null, never 0); no-re-alignment via comment-stripped static import scan; hand-computed scores (all-pass 1.0, 5/6 mix, gates-only 0.5). No live run-through (offline tests only).

## Phase 3 — Suites & mass-run
- [x] WP 3.1 — contract + schema: suites, suite_tests/scenarios, suite_runs, runs linkage — done 2026-07-04 · wp/benchmarks/3.1 (4c01182). Front-loaded suite contract (`Suite`/`SuiteConfig`/`SuiteVariant`(5.1)/`SuiteRun`/`SuiteCell`/`SuiteAggregates`/`SuiteAnalytics`/`SuiteScatterPoint`/`SuiteBreakdownSlice`/`FailureBucket`(3.5)/`SuiteDeltaRow`(5.1) + zod + `SUITE_*` constants); migration **v14** (suites/suite_tests/suite_scenarios/suite_runs; `runs += suite_run_id, repetition`); `SuiteRepository`/`Service`/routes (CRUD). Gate green on merged main (typecheck · test **535/535**, +6 · build · lint). **Invariant proven:** deleting a suite cascades its `suite_runs` aggregate rows but keeps child `runs` (`runs.suite_run_id` is deliberately NOT an FK — runs are history). Not verified: orchestration/analytics/variants (front-loaded types, unconsumed by design — WP 3.2–3.5/5.1); no live-server exercise (contract WP).
- [x] WP 3.2 — orchestrator: cells, concurrency, repetitions, soft-stop cap, suite SSE, stop/reconcile — done 2026-07-04 · wp/benchmarks/3.2 (245f79d; merged 7388387). `SuiteOrchestrator` (test×scenario×rep matrix through a bounded worker pool; each cell = `runService.start` — a normal persisted run; injectable start/stop for offline tests); `SuiteRunManager` (SSE, mirrors run-manager bounded-buffer/seq); `SuiteRunRepository` (config snapshot, finalize→cached `aggregates_json`, `reconcileOrphans`, delete-unlinks-children); additive `RunRepository.linkRunToSuite`; routes `POST /api/suites/:id/run`, `GET /api/suite-runs[/:id]`, `/:id/stream`, `/:id/stop`, `DELETE /:id`. Gate green on merged main (typecheck · test **601/601**, +7 · build · lint). **All 7 invariants proven** (matrix-once/cell, concurrency bound, cap soft-stop w/ in-flight drain, stop, orphan→error, aggregates recompute==cache, delete-keeps-children). Aggregation documented: primary-grader priority (`outcome_judge`→…), no-score runs excluded (never 0), population stddev, judge cost cumulative. **Accepted deviations:** soft-stop cap sums SETTLED child cost (in-flight not queryable mid-run — bounded overshoot, within soft-stop spirit); `SuiteRunEvent` SSE envelope in `apps/api` (WP 3.3 lifts it to `shared`). `index.ts` route conflict vs 1.3 resolved. Not verified: live SSE/EventSource (offline tests only).
- [x] WP 3.3 — suite console UI: matrix grid, KPI rail, drill-through, suite CRUD — done 2026-07-04 · wp/benchmarks/3.3 (9da901d; merged e9382c3). Additive `SuiteRunEvent` lifted to `packages/shared` (sole W4 shared writer); `/testing/suites` (list + `SuiteEditor`: ordered test picker w/ up-down reorder — no DnD dep, scenario multi-select, config form) + `/testing/suite-runs/:suiteRunId` (matrix grid on `@elabs-ai/components-*` `Table`, `MetricCard` KPI rail w/ exec+judge cost tiles, drill-through to child run console); `use-suite-stream` mirrors `use-run-stream` (idempotent cell upsert by `cellKey`, terminal-only errors, `seq` dedup); Suites nav item. Props confirmed via brand-ui MCP. Gate green on merged main (typecheck · test **646/646** · build · lint). **Visual (both themes, `:8099`):** suites list + suite editor render correctly and **read cleanly in light AND dark** (good contrast, tokens flip, "2× reps · 3 parallel · $0.50 cap · 2 cells"). Honest UX limit surfaced in-product: a finished run reopened shows the aggregates rail + note (per-cell events aren't persisted). **NOT verified (owner-acceptance — Phase 3 walk):** a LIVE suite run filling the matrix / cap soft-stop / scatter (needs a provider key); full a11y walk. `lib/api.ts` conflict vs 1.4 resolved.
- [x] WP 3.4 — analytics: metadata breakdowns, quality×cost scatter, suite report export — done 2026-07-04 · wp/benchmarks/3.4 (d6cf681; merged ebbe314). Pure `analytics.ts` (`computeSuiteAnalytics`: scatter points {testId,scenarioId,meanScore,meanTokens,meanCostUsd,reps} reps-averaged + breakdown slices by category/difficulty/tag × scenario; ungraded reps excluded, never 0; grader-dimension query, 404/empty honest); `GET /api/suite-runs/:id/analytics`; suite-run report (`GET /api/reports/suite-run/:id/{json,markdown}`, config snapshot + aggregates + per-cell + analytics). Web: Matrix/Breakdowns/Quality×cost tabs on `SuiteRunConsole` + report export. Gate green on merged main (typecheck · test **651/651**, +5 · build · lint, 385 files). Hand-computed analytics assertions verified. **⚠ Owner-gap flagged (NOT a STOP):** the vendored `@elabs-ai/components-charts` `ScatterChart` coerces its x through a `scaleTime` and exposes no per-point `onClick` — so the scatter renders Y=score axis + points positioned by X=tokens/cost with the X value in the tooltip + color-by-scenario + drill-through via a keyboard-navigable Subjects list, but the **numeric X axis is unlabeled**. A fully-labeled linear-x ScatterChart is a `@elabs-ai/components-charts` upstream enhancement (library-first: composed from `@elabs-ai/components-*`, didn't hand-roll). **Not verified:** charts not viewed live (need a seeded suite RUN — folded into the E2E walk); default scatter dimension = primary-grader priority.
- [x] WP 3.5 — failure buckets: opt-in LLM clustering of low-score judge reasons — done 2026-07-04 · wp/benchmarks/3.5 (2dd52ad, FF). `FailureBucketService` (reuses the WP 1.3 judge seam): gathers low-score grade rows (`score < FAILURE_BUCKET_SCORE_THRESHOLD`) + reasons → ONE clustering judge call → `FailureBucket[]`; unconfigured→400, unpriced→400, provider fail/timeout→502 (bounded, never hangs). `POST /api/suite-runs/:id/failure-buckets` (the SOLE caller); persisted on `suite_runs.aggregates_json` (derived). Web: 4th "Failure buckets" tab (cost-notice, explicit trigger, cluster table + member drill-down). Gate green on merged main (typecheck · test **658/658**, +7 · build · lint, 388 files). **Invariants proven:** membership integrity (hallucinated + passing run ids dropped, shares sum to 1); cost on the grading ledger only (`aggregates.judgeCostUsd`; every `runs.cost_usd` + all `run_grades` byte-identical); re-trigger overwrites (base recomputed from grades — no double-count); **never unprompted** (static scan of orchestrator/run-service/grade-service asserts no auto-trigger; route is the sole caller). Not verified: no live UI/themes (offline tests only — folded into E2E).

## Phase 4 — Collections & GitHub two-way sync
- [x] WP 4.1 — contract + schema: collections, membership, on-disk format + externalKey, serializers — done 2026-07-04 · wp/benchmarks/4.1 (2690896; merged 10f24ae). `Collection` (redacted `hasPat`), on-disk `TestFile`/`SuiteFile` (`externalKey`, `formatVersion:1`), sync-state/`SyncConflict` shapes (front-loaded for 4.2/4.3) + zod; migration **v15** (`collections`; `tests/suites += collection_id` FK ON DELETE SET NULL + `external_key`; per-collection partial-unique indexes); `CollectionRepository` (PAT via `SecretStore`, redacted, `decryptPat` internal), `serializer.ts` (deterministic key-sorted / 2-space / trailing-newline; no local ids/refs/secrets). Gate green on merged main (typecheck · test **564/564**, +9 · build · lint). **Invariants proven:** PAT absent from every response (raw + `enc:v1` blob greps); golden-file byte-stable, no local refs; attachments → `warnings:["attachments-not-synced"]`; re-key on move. **Good catch:** partial-unique indexes moved to v15-only (a pre-v15 upgrade crashed on `schema.ts` referencing not-yet-added columns) — verified fresh + rewound. `index.ts` route conflict vs 1.2 resolved. Not verified: no git (WP 4.2); no live HTTP.
- [x] WP 4.2 — git engine: working clones, export/commit, fetch+merge, conflicts, PAT discipline — done 2026-07-04 · wp/benchmarks/4.2 (95ce4fb; merged 8ffbc88). **One-implementation consolidation**: extracted `git/git-credential.ts` (argv-only PAT, `credential.helper=`/`GIT_TERMINAL_PROMPT=0`, SSRF DNS guard via `isBlockedIp`, subprocess timeout, `redactUrl`) now shared by skills-git + skills-publish + collections-sync. `CollectionGitSyncService`: working clone at `DATA_DIR/collections/<id>` (origin scrubbed tokenless — PAT never in `.git/config`), export→commit→fetch→merge (`git merge` only — NEVER rebase/force-push), conflicts from porcelain + index stages `:2`/`:3`, `resolve` take-local/take-remote/edited; import-bootstrap; `POST /:id/sync`, `GET /:id/status`, `POST /:id/resolve`. Gate green on merged main (typecheck · test **612/612**, +11; pre-existing skills-git 14 tests unchanged · build · lint). **Invariants proven (offline `file://` matrix):** PAT absent from responses AND captured logs; **no `--force`/`--force-with-lease`/`rebase` code path** (argv grep); zod-invalid remote → clean fail naming the file, DB untouched (transactional); idempotent `unchanged`. Remote-delete-that-merges → detaches member (history-preserving, documented). `index.ts` route conflict resolved. **⚠ NOT verified (owner-acceptance):** live-GitHub HTTPS+PAT round-trip + real 401 mapping (offline only).
- [x] WP 4.3 — sync UI: collection manager, diff preview, conflict resolution — done 2026-07-04 · wp/benchmarks/4.3 (943155e, FF). `features/testing/collections/` (list w/ lazy sync-state chips, create/bind wizard, `CollectionDetail`, `ConflictResolution` via `@elabs-ai/components-editor` DiffEditor/CodeEditor, `ImportInsightBenchDialog` — the folded-in WP 4.4 web entry); `/testing/collections[/:id]` routes + Collections nav; collection picker in test/suite editors. Props confirmed via brand-ui MCP. Gate green on merged main (typecheck · test **646/646** · build · lint, 379 files). **PAT write-only** (`type=password`+`autocomplete=off`, seeded empty, "PAT set" badge from `hasPat`, never read back); destructive picks `AlertDialog`-gated. **Two honest API-shape limits (documented, polish follow-ups — not blockers):** (1) `Test`/`Suite` list responses don't hydrate `collectionId`/`externalKey` and `Collection` exposes no member list → editors do immediate-write assign/detach; a full member read-back needs an additive `collectionId` on those responses. (2) `GET /:id/status` returns ahead/behind/dirty/conflicts (no per-file add/change/remove tree) → diff preview is the state strip + per-file conflict diffs. **NOT verified (E2E walk + owner-acceptance):** app-run/both-themes + the LIVE bare-repo bind→sync→conflict→resolve→converge round-trip (deferred to the post-W6 E2E walk).
- [x] WP 4.4 — InsightBench importer (one-way) — done 2026-07-04 · wp/benchmarks/4.4 (17c9891; merged 5f66370). `InsightBenchImporter` + `POST /api/collections/import/insightbench` (body-based, no multipart dep): app→`tags`+category, `acme_question`→userPrompt, `gt_insight`/`gt_insight_value`→expectations, `gt_code`→`referenceLogic{code,python}`; `DIFF_LEVEL_MAP` (1–2 easy/3 medium/4 hard) + unanswerable-insight regex ported from `convert_to_benchmarks.py` → `answerable:false`; one suite per import; SHA-256 content-hash dedup (idempotent re-import). Trimmed real fixture (3 apps/5 questions incl. a difficulty-4 + two `answerable:false`). Gate green on merged main (typecheck · test **646/646**, +5 · build · lint). **Invariants proven:** idempotent re-import (created 0/skipped 5, same suiteId); no exporter (static key scan); exact `referenceLogic.body` + `answerable:false`. **Per decision-log 2026-07-04, the web Import button folds into WP 4.3** — this WP is API+fixture only. Documented judgment calls: group-level difficulty inheritance; collection assignment only on newly-created rows. Not verified: no live HTTP POST (offline importer/repo tests only).

## Phase 5 — Skill-effect A/B
- [x] WP 5.1 — suite variant axis (± skill / version pin) + per-test delta view — done 2026-07-04 · wp/benchmarks/5.1 (5ceb7df; merged e9d61f3). Additive-only seam: `resolveAllowedSkills(scenarioId, overrides?)` (`applySkillOverrides` attach/detach/pin — scenario row never mutated) → `run-service.start(…, skillOverrides?)` (sole W6 run-service edit; validates at start) → orchestrator variant axis (`test × variant × rep` when `config.variants` set; each cell stamps `variantLabel`+overrides); `GET /api/suite-runs/:id/deltas?base=<label>` (`SuiteDeltaRow[]`); web Variants section (`SuiteEditor`, reuses `AddSkillModal`) + Skill-effect Delta tab. Gate green on merged main (typecheck · test **666/666**, +8 · build · lint, 390 files). **Invariants proven:** matrix cardinality (8 cells, variantLabel); override resolution (base run records NO skill, +variant records it — from `run_skills`); hand-computed deltas (pure + end-to-end); **deleted-skill variant fails at run START** (typed 400 before any cell schedules); variants ride `config_snapshot_json` (replay-exact). `suites/routes.ts`/`SuiteRunConsole.tsx`/`lib/api.ts` conflicts vs 3.5 resolved. **Documented gaps (follow-ups, not blockers):** the "Trace-tab deep-link for fractured runs" is unimplemented — `SuiteDeltaRow` is aggregate/mean-over-reps with no run id (design consequence of the front-loaded shape); the Quality×cost scatter (3.4) collapses same-scenario variants into one point — the Delta tab is the correct variant-comparison surface. **Not verified:** no live UI/themes (offline tests only — folded into E2E).

## Phase 6 — Judge calibration & trust · BACKLOG (owner-added 2026-07-04 — NOT in the W1–W6 mission; do not pick up without owner instruction)
- [ ] WP 6.1 — grade feedback + calibration set (spec: `phase-6-judge-calibration.md`)
- [ ] WP 6.2 — agreement analytics + judge-change re-grade guard

## Decision log
_Owner-of-record: Claude (Benchmarks tech lead). Each entry: date · decision · rationale. All
within the locked bounds of B1–B15; none is a STOP item (§1 of the mission)._

- **2026-07-04 · Migration numbers claimed up front.** `main` is at `user_version = 12`; the
  parallel Skill IDE workstream carries **no** in-flight schema migration (its done WPs 1.1/1.2/3.1
  and remaining specs operate on graph projection / immutable skill versions, not `schema.ts`).
  Benchmarks claims: **v13** = WP 1.1 (`tests += expectations_json, category, difficulty,
  tags_json`; new `run_grades`), **v14** = WP 3.1 (`suites`, `suite_tests`, `suite_scenarios`,
  `suite_runs`; `runs += suite_run_id, repetition`), **v15** = WP 4.1 (`collections`;
  `tests/suites += collection_id, external_key`). If Skill IDE later needs a migration it takes
  v16+. _Rationale: never two migrations with the same number across parallel workstreams._
- **2026-07-04 · Contracts are front-loaded (single shared-writer per file).** Each contract WP
  lands its ENTIRE downstream wire surface so no later WP re-touches `packages/shared`: WP 1.1 =
  all `GraderId` members (`rouge1`, `value_match`, `outcome_judge`, `tool_hygiene`,
  `trajectory_judge`, `skillflow_conformance`) + `ToolHygieneFinding` + judge `method` enum; WP
  3.1 = suite/cell/aggregate + quality×cost scatter-point + analytics-slice + `SuiteVariant` (5.1)
  + `FailureBucket` (3.5) shapes; WP 4.1 = collection + on-disk file-format + sync-state + conflict
  shapes. _Rationale: contract-first discipline eliminates serialized-file contention in W3–W6._
- **2026-07-04 · Judge settings persist in a generic `app_settings` KV table, created in v13.**
  No server-side settings table exists today. WP 1.1 (v13) adds a minimal
  `app_settings(key PRIMARY KEY, value_json, updated_at)` table; WP 1.3 reads/writes the default
  judge (`providerCredentialId` + `model`, references only — never key material) under key
  `'judge'`. _Rationale: keeps every schema change inside the three contract migrations (v13–v15);
  WP 1.3 needs no migration in W3. Within B3's locked scope (default judge stored like other app
  settings, references only)._
- **2026-07-04 · Outcome LLM judge grader id = `outcome_judge`.** The specs name the deterministic
  graders and the process graders but not the B3 outcome judge's id. _Rationale: descriptive,
  stable, distinct from `trajectory_judge`._
- **2026-07-04 · WP 4.4 split: API+fixture in W4, UI button folds into WP 4.3 (W5).** WP 4.4's
  spec'd web entry point ("Import action on the collections screen") targets a screen that WP 4.3
  builds in a LATER wave — a dependency inversion. WP 4.4 ships its API importer + fixture test in
  W4 (its Acceptance is entirely API/fixture); the collections-screen Import button lands with WP
  4.3. README WP-index + ledger updated together. _Rationale: keeps each WP independently
  gate-clean; no WP blocks on a not-yet-built screen._
- **2026-07-04 · W2 serialized-file merge order.** WP 3.1 and 4.1 both write
  `packages/shared` + `schema.ts` + `database.ts`. They develop in parallel worktrees (v14/v15
  claimed up front) but MERGE strictly sequentially **1.2 → 3.1 → 4.1**, each rebased on `main`
  immediately before its merge; the orchestrator resolves the mechanical `MIGRATIONS`-array /
  `CREATE TABLE` append conflicts. _Rationale: one writer at merge time honors the serialized-file
  rule while preserving parallel wall-clock._
- **2026-07-04 · Integration target = local `main`, no origin push.** Per the mission merge
  protocol, WP branches merge into local `main` with a full gate after each merge. A concurrent
  owner auto-commit/push session is active in this repo, so every merge is preceded by
  `git fetch` + a `main`/`origin/main` movement check; **never force-push; no origin push without
  owner sign-off.** _Rationale: reconciles the mission's on-main integration with the known
  concurrent-session hazard._

## E2E validation (2026-07-04 · orchestrator-run, no provider key available)
Run against the FINAL merged build (`main@e9d61f3`) served from a fresh isolated DB on `:8099`
(migrations applied cleanly to `user_version = 15`; server health 200 throughout; the only two
error-level log lines were the deliberately-tested honest 404/400 paths). What NEEDS a live
provider key is seeded/marked owner-acceptance and called out first.

**⏸ Needs a provider key (owner-acceptance, could NOT run live):** a genuine agent run → LLM-judge
grade; a live suite run filling the matrix + cap soft-stop; a live skill-effect ± skill delta. The
LLM-judge, suite-execution, and delta *computation* are covered by the offline test suite (666
tests); only the live execution is unverified.

**✅ Verified LIVE (provider-free):**
- **MCP scan** — registered a stdio `server-everything` stub, scanned it → `success`, **13 tools /
  1423 tokens** (real `initialize`+`tools/list`; this scan backs `tool_hygiene`).
- **Deterministic grading via the real `POST /api/runs/:id/grade`** — on a completed run whose final
  message states the insight + emits the expected JSON: `rouge1 = 0.89` (real unigram-F1),
  `value_match = 1.00` (parsed + matched `expectedValue`), `tool_hygiene`/`skillflow_conformance` =
  `unevaluable`, `outcome_judge` = `unevaluable(judge_unconfigured)`, and `trajectory_judge` **wrote
  NO row** (its `appliesTo` skipped it — no `referenceLogic`). **Run `cost_usd` byte-identical
  (0.009) before/after** — grading never touches run cost.
- **Grade panel (WP 1.4)** — visually confirmed earlier in both themes on the seeded run (ROUGE-1
  42% / Value-match 100% / Tool-hygiene "Not evaluable" muted / Outcome 80% + reasoning / Trajectory
  70% / Re-grade).
- **Suite analytics (WP 3.4)** — on a seeded 2-cell suite run: scatter `(0.9, 350tok, $0.02)` +
  `(0.3, 550tok, $0.05)` (meanTokens = in+out, exact), 6 breakdown slices; `/deltas` (no variants) →
  honest `[]`; suite-run report markdown → 200; unknown suite-run → **404**.
- **Failure buckets (WP 3.5)** — with a low-score grade but no judge → honest **400** ("No default
  judge is configured…"), never crashes; with no low-score grades → 200 empty (no judge call).
- **Collection two-way git sync (WP 4.1/4.2/4.3)** — bound a collection to a local `file://` bare
  repo (`hasPat:false`, **no PAT in any response**) → assigned a test → **sync = `pushed`** (file
  landed at `bench/tests/revenue-by-region.json` with **no secret / no local id** — externalKey
  identity) → edited the same field REMOTE (clone+push) AND LOCAL (PUT) → **sync = `conflicts`** on
  that file (both sides parsed) → **`resolve` take-remote = `merged`** → re-sync **converged**
  (`{ahead:0,behind:0,dirty:false}`; local DB test + bare repo both carry the remote content; **no
  duplicate test** — updated in place).
- **InsightBench importer (WP 4.4)** — imported the fixture → **5 tests + 1 suite**; re-import
  **idempotent** (created 0 / skipped 5); a spot-checked test carries `referenceLogic{python}`,
  `difficulty:hard`, `tags:["flag-1"]`; **2 tests `answerable:false`**.

## Owner acceptance (deferred visual / a11y / live-credential — owner-only)
- [ ] Phase 1 — two-theme walk: test with expectations → run → Grade panel (scores, judge
      reasoning, re-grade); judge configured in Settings against a real provider — accepted: ____
- [ ] Phase 2 — a run with a deliberate wrong-parameter tool call shows tool_hygiene findings;
      a trajectory-judge grade cites real steps — accepted: ____
- [ ] Phase 3 — live suite run: matrix fills, cap soft-stops, scatter + breakdowns read
      correctly in both themes — accepted: ____
- [ ] Phase 4 — live GitHub round trip on a private repo with PAT: bind → sync → conflict →
      resolve → converge; PAT never surfaces — accepted: ____
- [ ] Phase 5 — a ± skill delta table on a real suite — accepted: ____
