# hub-fixes — work-package status ledger

Living state for the `hub-fixes` plan ([README](./README.md) · [conventions](./conventions.md) ·
[analysis](./analysis.md) · [kickoff](./kickoff-prompt.md)), read and updated by the orchestrator /
the `next-wp` skill. A box is ticked **only** when the WP's Acceptance is met and the gate is green
(`pnpm typecheck && pnpm test && pnpm lint`; `pnpm build` at batch integration).

**Legend:** `[ ]` open · `[x]` done. A trailing `status:` note marks `in progress` / `in review` /
`blocked`. Done lines record the date + branch: `… — done YYYY-MM-DD · wp/hub-fixes/<id>`.

**Decisions adopted 2026-07-19 (owner planning session): D-HF1…D-HF7 — see README. Revisit only
before the affected WP starts; note any change here.**
**Evidence base: [`analysis.md`](./analysis.md) (RC1…RC7), verified against the working tree and the
live instance (session `oNiw1PCAmxc5_ietGD_0h`) on 2026-07-19.**

## Gates — check before any implementation batch

- [ ] Working tree clean enough for worktrees (`git status`; commit/stash strays) — checked at each batch.
- [ ] No other workstream is editing `apps/api/src/hub/**` or `apps/web/src/features/hub/**` concurrently (assistant-hub and assistant-hub-ux are COMPLETE; if anything new started, exchange STATUS links first).
- Owner-op note: WP 0.1 changes `docker-compose.yml`; the owner recreates the container to apply it.

## Phase 0 — same-day mitigation
- [x] WP 0.1 — eager-mode mitigation + scoped-session runbook — done 2026-07-19 · wp/hub-fixes/0.1 (gate green; owner-op: recreate container to apply, live scoped-Qlik proof → owner-acceptance)

## Phase 1 — MCP truth in main sessions
- [x] WP 1.1 — deferred-tool promotion + `auto` loading policy (Opus) — done 2026-07-19 · wp/hub-fixes/1.1 (RC1; native `ai@7` `prepareStep`/`activeTools` gating — deferred MCP tools are now BUILT into the callable map and gated off each step until a token-capped `tool_search` promotes them; ungranted tools structurally unpromotable [negative test]; `auto` default flip [eager under `min(ctx·frac, 40k)`, else deferred-promote]; eager path byte-identical. **FOLLOW-UP → WP 1.3:** thread `config.hubToolEagerMaxTokens`/`hubToolPromoteMaxTokens` through `index.ts` — the env OVERRIDES are currently inert, defaults [40k/20k] active.)
- [x] WP 1.2 — scope plumbing honesty: persist, PATCH, grant-aware rail, builtins (Sonnet) — done 2026-07-19 · wp/hub-fixes/1.2 (RC3 display half + write-once trap; additive shared `toolScope`/`scopeMode`; inspector reads `session.toolScope` + `scopeMode`; PATCH persists scope; `resolveHubMcpGrants` honors `scope.builtins` [empty→defaults]; `ManageToolScopeDialog` reuses `ToolGrantPicker`. Both-theme rail/dialog look → owner-acceptance.)
- [x] WP 1.3 — MCP connection status surfacing: events, chips, retry (Sonnet) — done 2026-07-19 · wp/hub-fixes/1.3 (RC3.4; additive `mcp_server_status` `HubEvent` + `HubContextToolsLayer.serverStatuses?`; `resolveHubMcpGrants` no longer returns silent `null` on all-fail — carries per-server outcomes → deduped status events + `Unreachable this turn` prompt line; cache extracted to unit-tested `HubResourcePool`; `POST /api/hub/servers/:id/reconnect` evicts; rail chips + Retry. **WP 1.1 env-threading FOLLOW-UP wired** (`eagerMaxTokens`/`promoteMaxTokens` in the `HubSessionService` construction). **Deferred sub-item:** the inline transcript notice is data-only (`use-hub-stream` `turn.mcpServerIssues`); its render needs `ConversationPane.tsx` (WP 3.1's file) → carried to the next ConversationPane WP (6.1). Not an Acceptance item. Both-theme chip/Retry look + real failing-MCP path → owner-acceptance.)
- [x] WP 1.4 — tools prompt-budget compression (Sonnet) — done 2026-07-19 · wp/hub-fixes/1.4 (deferred `formatToolListText` → one summary line per server [`N tools (searchable) — e.g. …`]; eager unchanged; WP 1.1 registry additions untouched; `HUB_PROMPT_SECTION_BUDGETS.tools` 400→700; measured 5-server/280-tool section ~2715→544 tokens. Merged 34a9d28, integrated typecheck green.)

## Phase 2 — mission agents become real sessions
- [x] WP 2.1 — turn-engine agent runner (Opus; ran SOLO) — done 2026-07-19 · wp/hub-fixes/2.1 (RC2; children are now REAL child hub sessions: created with `toolScope: planned.toolGrants`, run through `session-service.runAgentTurn` → the same turn engine [granted MCP tools callable via WP 1.2 honor + WP 1.1 loading], role prompt as additive `turn-engine systemPromptOverride`, tool calls + real usage/cost persisted in the CHILD log, then a bounded `generateObject` extracts `hubAgentReport`. **Production default = new `session` runner** (`readHubAgentRunner`→`session`; `index.ts` wires `createSessionAgentRunner`); `HUB_AGENT_RUNNER=structured` keeps the old one-shot byte-compatible [D-HF7]. Real cost = child turn + extraction, no hardcoded 0. Minimal mission-`auto` approvals: read-only MCP auto-runs, rest gated-closed + auto-declined + report note [full policy → WP 2.5]. Child grants pass-through [D-HF5 minimal; intersection → WP 2.3]. Planner/topologies/budgets/board untouched. Additive shared/turn-engine only. Merged d426068. **Full gate GREEN** (typecheck · API 2884/2884 + web 2435/5skip · lint · build). **e2e:** WP 2.1's own mission Playwright test PASSES (child calls a granted read-only stdio MCP tool → `tool_call`+`tool_result` → report → synthesis); the WP4.4/4.1 hub-UI e2e local failures are PRE-EXISTING (verified: WP4.4 chat-artifact fails on the pre-2.1 baseline too; choreography flakes) — CI is the reference for those. Real-provider/live-MCP runner path → owner-acceptance.)
- [x] WP 2.2 — planner server catalog + plan-card grant editing + role warnings (Opus) — done 2026-07-19 · wp/hub-fixes/2.2 (RC2.4; `buildPlannerServerCatalog` + "## Grantable MCP servers" least-privilege injection [scope-aware, same source as `resolveHubMcpGrants`]; loud `clampGrantsToCatalog` strips hallucinated server ids with an idempotent plan note [live on the EDIT route]; `MissionPlanCard` per-agent server chips + `ToolGrantPicker`-constrained edit + unconfigured-role warning [shared `HUB_ROLE_PLACEHOLDER_MARKERS` const + web mirror]; no `packages/shared` change. Merged fc1a46d, typecheck green. **CARRY-FORWARD → WP 2.3:** the PROPOSE-path runtime wiring (planner prompt catalog injection + initial clamp catalog at `orchestrator.ts`'s `runPlanner` call site — forbidden to 2.2) + the end-to-end "stub plan proposes non-empty grants for a data question" test. Functions are catalog-ready + tested; it's a small hookup in the file 2.3 owns. Both-theme plan-card look → owner-acceptance.)
- [x] WP 2.3 — grant inheritance rule + plan validation (Sonnet) — done 2026-07-19 · wp/hub-fixes/2.3 (D-HF5; pure `effectiveAgentGrants(planGrants, parentScope)` in `hub/tools/grants.ts` = plan grants ∩ parent scope [null parent ⇒ as-is; server-absent ⇒ dropped; `all`/list intersections; builtins with empty-parent="unrestricted" per RC3.5], applied at the child `createSession` spawn seam so a scoped parent can't be escalated by a crew role's Access tab [10-case table test + spawn NEGATIVE test]; `MissionPlanCard` effective-grant subtitle; intersection-removed note via the existing `agent_spawned.brief` channel [no new event]. **WP 2.2 CARRY-FORWARD DONE:** `proposePlan` now builds `buildPlannerServerCatalog` from a new `mcpCatalog` dep, injects the "Grantable MCP servers" section into the planner prompt + strips hallucinated ids at PROPOSE time; end-to-end "propose → non-empty grants" stub test green. **Composition-root activation:** orchestrator `resolveReachableMcpCatalog` wired at `index.ts` (`1a3aa07`) — the propose-path grant proposal is now LIVE (was stub-only). Merged e3f16ee. Full gate GREEN (typecheck · API 2922/2922 · web 2465/5skip · lint · build). Deferred (cosmetic): `MissionPlanCard.parentScope` prop not yet threaded from `ConversationPane` → carried to WP 6.1 (mirrors the pre-existing `catalogServerIds` gap). Both-theme card look + LIVE planner-proposes-grants → owner-acceptance.)
- [x] WP 2.4 — mission cost/budget integrity (Sonnet) — done 2026-07-19 · wp/hub-fixes/2.4 (side-findings; real per-agent cost/tokens now flow from 2.1's runner onto the mission rollup + `agent_report` [additive shared `costUsd?/tokensIn?/tokensOut?`, replay-safe]; fixed a real gap — the report-extraction call cost is now synced onto the child session row so `hub/usage.ts` rollups agree with the board; budget trip already fired correctly post-2.1 [`topologies.ts` untouched — no bug]; planner `estimateAgentCostUsd`/`estimatePlanCostUsd` = token-envelope heuristic priced via existing `estimateCost`, never 0 for a priced non-empty plan, honest 0 for unpriced; `MissionBoard` shows real spent/max bar + per-agent cost badge + Status Cost/Tokens rows, HONEST ABSENCE for pre-fix logs [no fabricated spend, tested]. Merged 8c5eaeb, typecheck green. Both-theme spend display + real-provider cost accuracy → owner-acceptance.)
- [x] WP 2.5 — mission HITL approval policy (Opus) — done 2026-07-19 · wp/hub-fixes/2.5 (D-HF6; `approval-policy.ts` `resolveMissionApprovalGate` [first-party never gates; `always_ask` gates every MCP call; `auto`/`threshold` auto-run ONLY read-only-and-not-destructive, gate the rest; annotations only TIGHTEN]; `always_ask` mirrors child approvals onto the PARENT log as additive `agent_approval_requested`/`agent_approval_responded` events → board approval queue [`MissionApprovalQueue` in `MissionBoard`, reuses `ApprovalCard`+`decideHubApproval`]; approve resumes the agent's slot, DENY → `output-denied` + honest report note [never fabricates]; injectable-timer `HUB_MISSION_APPROVAL_TIMEOUT_S` [300s] auto-deny + visible note so missions always terminate; `auto`/`threshold` get no sink [byte-compatible with 2.1]. `session-service.ts` change = relocating `buildMissionAgentHitl` into `approval-policy.ts` + minimal rewiring [3.2 rebases]; turn-engine untouched. Existing deny-never-runs invariants re-run + hold. Merged aea5ec1. Full gate GREEN (typecheck · API 2935/2935 · web 2490/5skip · lint · build). Both-theme queue look + live approval round-trip → owner-acceptance.)

## Phase 3 — rendering
- [x] WP 3.1 — markdown + inline citation chips together (Sonnet) — done 2026-07-19 · wp/hub-fixes/3.1 (RC4 rendering; chips woven via Streamdown `components` override, tables re-pointed at `@brand/ui` Table*; uncited byte-identical; hostile-title + orphan-marker tests green. Deviation: kept `ChatMarkdown.tsx` un-promoted to stay in exclusive files — noted. Nested-`[n]`-in-emphasis un-woven = documented limit. Both-theme chip-contrast look → owner-acceptance.)
- [x] WP 3.2 — synthesis through the turn engine with GenUI (Opus) — done 2026-07-20 · wp/hub-fixes/3.2 (RC4 second half, D-HF4; new `session-service.runSynthesisTurn` [modeled on 2.1's `runAgentTurn`, bounded, slot-cap-bypassing] runs mission synthesis as a REAL parent-session turn with GenUI `present`/`prompt_user` granted + MCP NOT granted [`resolveSynthesisToolset`]; settled `assistant_message` may carry a genui widget [renders via the existing `GenUiPart` path] + merged/re-numbered citations [§1.7 unchanged] + `mission_synthesis` marker [same messageId linkage, replay-compatible both orderings]; `HUB_SYNTHESIS_MODE=text` OR any failure ⇒ untouched byte-compatible `generateText` fallback, guarded against double-persist [`runSynthesisTurn` throws when it persisted no message]. session-service purely additive [no existing method modified — 2.5 rebase clean]. Merged 6e4e529. Full gate GREEN (typecheck · API 2940/2940 · web 2490/5skip · lint · build); own synthesis-genui e2e PASSES [WP4.4 UI walk is the pre-existing baseline flake]. Real-provider GenUI synthesis + both-theme widget render → owner-acceptance.)

## Phase 4 — mission board UX
- [x] WP 4.1 — truthful topology graphs, board + org-chart unified (Sonnet) — done 2026-07-19 · wp/hub-fixes/4.1 (RC6.1; `deriveDebateGraph` = one debater row + directed "sees + rebuts" chain, every debater → `Synthesis (resolver)` terminal; org-chart debate falls through to pipeline's directed chain [facing-pairs killed]; `TOPOLOGY_LEGEND` + unified `TOPOLOGY_EDGE_MEANING`; `TopoGraphEdge.label?` web-local additive; `TopologyGraph` props unchanged. MissionBoard NOT touched. Merged 8121a76, typecheck green. Both-theme graph/legend look → owner-acceptance; WP 4.4 round-structure extension point left in `deriveDebateGraph`.)
- [x] WP 4.2 — mission agent grid + detail box (Sonnet) — done 2026-07-19 · wp/hub-fixes/4.2 (RC6.3; reported agents → `grid sm:grid-cols-2` selectable cards [full a11y: `role=button`/`aria-pressed`/Enter-Space/`focus-visible:ring-ring`, mirrors sanctioned `AgentCard.tsx`]; first-reported default; `@brand/ui` Tabs detail box [Status via `Descriptions` + Report with Show-more]; per-agent cost deliberately omitted [RC2 hardcodes 0 → WP 2.4]; `spawnedAt`/`reportedAt` additive in `reconstructMissionBoard`; tokens only. Merged 5084efa, typecheck green. Both-theme look + hover/keyboard feel → owner-acceptance.)
- [x] WP 4.3 — expand modal + per-agent live session panel (Opus) — done 2026-07-19 · wp/hub-fixes/4.3 (RC6.2/6.4; `Maximize2` → `Dialog size="full"` [`MissionExpandDialog`], `TopologyGraph` gains additive `selectable`/`selectedNodeId`/`onSelectNode` [default false ⇒ inline board byte-identical]; new slim `AgentTranscript` fed by `use-hub-stream` [not readOnly ConversationPane — 6.1 contends it], streaming text + tool-call cards + unsubscribe-on-close/unmount [leak tests]; board detail box gains a Live tab; keyboard node-select via DOM `data-id`. Merged 46aff07, typecheck green. ConversationPane/orchestrator/API untouched. Both-theme + overflow@1280/1920 visual → owner-acceptance.)
- [x] WP 4.4 — round-based debate: parallel openings + rebuttals (Opus) — done 2026-07-20 · wp/hub-fixes/4.4 (D-HF3, RC6.1; `runDebate` now round-based — round 1 = all debaters PARALLEL on the bare brief [overlap-proven], round 2+ = parallel again each seeing every OTHER debater's opening via `composeDebateRoundBrief` [never its own], synthesis resolves; additive `debateRounds` [zod int 1..3, default 2, clamped]; dropped-debater flagged + drops out [never halts], budget trip checked at ROUND boundaries → correct `partial`; each round's report is the existing `agent_report`; `deriveDebateGraph` renders openings/rebuttal rows + "rebuts" edges [built on WP 4.1's parameterized seam]; OLD sequential-debate logs still replay [fixture]. orchestrator touch = 2 lines [conditional `debateRounds` spread; runDebate stays in topologies]. Merged 91c2b9b. **Follow-up (a) FIXED at integration** (`adf7977`): `missionBoardToTopoInput` now threads `debateRounds` so the LIVE graph is truthful for 1/3-round missions. **Follow-up (b, minor/documented):** selecting a REBUTTAL node (`::r{n}` id) in the expand modal falls back to the default agent gracefully [no crash] — the node→agent map in `MissionExpandDialog` doesn't include rebuttal ids; a future refinement. Full gate GREEN (typecheck · API 2946/2946 · web 2494/5skip · lint · build). Real-provider multi-round debate + both-theme graph → owner-acceptance.)

## Phase 5 — internet capability
- [x] WP 5.1 — `web.search` / `web.fetch` built-ins, provider-native (Opus) — done 2026-07-19 · wp/hub-fixes/5.1 (RC5, D-HF2 revises D-AH10; `web.search` = provider-native tool per kind [spike-verified: anthropic `webSearch_20250305()`, openai `webSearch()`, google `googleSearch({})`, all `isProviderExecuted`; compatible/ollama absent + prompt says why], composes with WP 1.1 gating [never deferred] + WP 2.1 agents; `web.fetch` = guarded GET with defense-in-depth SSRF [v4+v6 CIDR deny-lists, per-hop re-resolve, connect-pinned-to-validated-IP anti-rebind, redirect-to-private re-validation, hop cap, byte cap, no cookies/auth, untrusted]; results → hub citations; grantable via `builtins` [default only for search-capable non-agent unscoped session, never silently for agents]; `HUB_WEB_TOOLS=off` kill-switch; cost = truthful per-turn count `HubUsage.webSearches` [no fabricated $]. Additive shared. Merged 2415fe7. **Gate GREEN on a clean integrated worktree** (typecheck · API 2906/2906 · web 2458/5skip · lint · build) — run off-main because a concurrent assistant-hub-ux session left uncommitted WIP in main's tree. Live provider search + billing → owner-acceptance.)
- [x] WP 5.2 — research-server onboarding surfacing (Sonnet) — done 2026-07-19 · wp/hub-fixes/5.2 (RC5; `MissionPlanCard` web-capability notice + compact `ConversationPane` research hint on NON-empty transcripts, gated on `research mode + no research MCP server + no web capability`; acknowledges WP 5.1's `web.search` [`providerSupportsWebSearch` mirror + narrow-mounted `useHubModelRoster` probe, never fires for ordinary sessions]; honest copy ["paste your own API key; none bundled", no dark patterns]; new props wired from the render site [no dark-prop debt]; ConversationPane change localized to the empty-state/hint block [scrim/markdown/citation untouched]. Merged f295ec1. Full gate GREEN (typecheck · API 2925/2925 · web 2485/5skip · lint · build). Caveat: the plan-card deep-link lands on `/servers` [one click from Add-server, whose connection step shows the research presets] rather than auto-opening the wizard — true one-click needs `App.tsx`'s global `ServerWizard` state (out of scope). **Follow-up:** a `?addServer=research` query-param auto-open in `App.tsx`. Both-theme look + live click-through → owner-acceptance.)

## Phase 6 — mode routing
- [x] WP 6.1 — `auto` session mode + chat-vs-mission clarify card (Opus) — done 2026-07-20 · wp/hub-fixes/6.1 (RC7; new `auto` session mode [additive to the mode union; `NewSessionDialog` default] routes per message — trivial ⇒ direct answer, decomposable ⇒ `mission.propose_plan`, ambiguous ⇒ GenUI `prompt_user` clarify card ["Quick answer / Run a mission (≈\$X, N agents)"], then acts on the choice; NEVER silently starts a mission [plan-approval autonomy gate still applies]; `proposePlan` gate lifted to accept `mission` OR `auto` [chat/research still never propose]; a minimal post-turn bridge in `dispatchMessage` hands an auto turn's `mission.propose_plan` call to the orchestrator [`echoUserMessage:false`, cycle-safe seam via `index.ts`]; `capabilities.ts`/`turn-engine.ts` UNTOUCHED. **Follow-up A DONE:** `AssistantTurnBlock` renders the WP 1.3 `turn.mcpServerIssues` unreachable-server `Alert` [terminal-only, `!turn.streaming`]. **Follow-up B DONE:** `parentScope`/`catalogServerIds` threaded into the `MissionPlanCard` render [WP 2.3 subtitle lit]. **Migration v51 (flagged deviation — genuinely unavoidable):** `hub_sessions.mode`'s CHECK rejected `'auto'`; widened via a self-guarded, idempotent, FK-safe 12-step table rebuild [all 4 indexes recreated, explicit column copy, dedicated test proves rows+self-FK survive + `auto` insertable + garbage rejected]; baseline `schema.ts` permits `auto`; 13 mechanical version-assertion bumps 50→51. Interrupted mid-run by an OAuth infra error, RESUMED same agent from intact partial work. Merged d484f20. Full gate GREEN (typecheck · API 2956/2956 · web 2500/5skip · lint · build); own auto-mode 3-route e2e PASSES. Both-theme Auto/clarify/notice look + live LLM routing → owner-acceptance.)
- [x] WP 6.2 — composer mode/autonomy clarity (Sonnet) — done 2026-07-20 · wp/hub-fixes/6.2 (RC7 composer half; `SessionModeChip` in the composer surfaces the session MODE [icon + label] beside the model chip and switches it [auto↔chat↔research; `mission` never an offered target — keeps create-time/crew semantics] via additive `hubSessionPatchSchema.mode` [mirrors 1.2's `toolScope`]; `routes.ts` PATCH persists `mode` [via `repository.updateSession`, extra file justified by the layering rule] with a running-mission guard [mission↔auto swap 409s while a mission is live, both directions, allowed once terminal]; `AutonomyModeSelect` gains an `Autonomy:` label prefix + 3-level tooltip [no functional change]. Additive shared. Merged 58a8269. Full gate GREEN (typecheck · API 2959/2959 · web 2510/5skip · lint · build). a11y: both chips are real focusable buttons w/ distinct aria-labels [jsdom-proven]; cross-browser Tab/Enter + both-theme chip look → owner-acceptance.)

## Phase 7 — review
- [x] WP 7.R — adversarial review + owner-acceptance walk assembly (Opus, ran SOLO) — done 2026-07-20 · wp/hub-fixes/7.R (all 7 invariants adversarially probed with forged plan JSON + hostile `tool_search` queries + the SSRF address/redirect matrix + hostile citation URLs — **all HOLD**; report [`review-7R.md`](./review-7R.md), walk [`owner-acceptance-walk.md`](./owner-acceptance-walk.md)). **No confirmed defect → no `7.fix` branch.** Delta is test-only + docs (zero product/engine/web-source change): new `apps/api/test/hub-wp7r-adversarial.test.ts` [10 probes: forged-plan grant confinement, hostile-query promotes-nothing, per-turn reset, per-search cap, SSRF v4-mapped/NAT64/decimal-hex-octal/name-resolve-time/redirect-to-scheme+creds] + 7 appended `SourcesPanel.test.tsx` probes [`safeHref` rejects `javascript:`/`data:`/`vbscript:` — the one untested rendering-safety edge, now locked]. **Two recorded, non-blocking:** ACCEPTED-RISK — promotion cap is per-SEARCH not per-turn [bounded by the 20-step cap + granted catalog; NOT a confinement break; model self-pays; single-owner/local; advisory budgets]; MINOR by-design — a granted-but-unscanned server isn't surfaced as a status [not a connection failure]. INV3 grep of the grant chain found NO silent swallow (only "nothing to connect" `return null`s). Full gate GREEN (typecheck · API 2969/2969 · web 2516 pass/5 skip · lint clean). e2e NOT run by 7.R [test+docs delta can't affect it; the WP4.4/4.1 hub-UI tests are the documented pre-existing macOS flakes — CI is the reference]. Everything provable only live/in-browser → the assembled owner-acceptance walk, nothing faked.

## Owner-acceptance (never faked; assembled by WP 7.R)

> **ASSEMBLED — see [`owner-acceptance-walk.md`](./owner-acceptance-walk.md)** (WP 7.R, 2026-07-20):
> the ONE honest live checklist with exact click-paths + expected outcomes, consolidating every item
> below plus the pre-flight decisions (migration v51 on deploy; the WP0.1 `docker-compose` eager-override
> **removal** decision now that WP1.1 shipped `auto`) and the full both-theme + keyboard walk. The raw
> items below are retained for provenance.

- Live scoped-session Qlik tool call (RC1/RC3 proof) — needs the running instance + Qlik OAuth. **Ready to verify after WP 0.1 merge (2026-07-19): recreate the container (`docker compose up -d --build`), create a session scoped to `qlik-mreimitz`, ask a question → a `qlik_*` call should appear with an approval card and return data once approved.**
- Live mission agent MCP call + streaming transcript in the expand modal.
- Both-theme walks: rail Tools section, board grid, expand modal, clarify card. **Batch 2 added surfaces to walk:** rail per-server connection chips + Retry (WP 1.3), mission agent grid + Status/Report detail box (WP 4.2), debate topology row + "sees + rebuts" edges + per-topology legend, board vs org-chart (WP 4.1).
- Real failing-MCP path end-to-end (WP 1.3): a broken/expired server → error chip + `Unreachable this turn` prompt line → Retry → next-turn reconnect. Needs a genuinely broken server or a live OAuth-expiry.
- WP 2.1: a real mission with a real provider key + a live MCP server — an agent calls a granted tool with a real transcript + real cost + budget enforcement. (Stub-proven in the gate; live is owner-acceptance.)
- Note: the WP4.4/4.1 hub-UI Playwright tests are flaky/broken on the local macOS box (pre-existing, verified against the pre-2.1 baseline) — treat CI as the reference for the full e2e suite, not local runs.
- `web.search` behind a real provider key on at least one provider.

## Orchestration log

- 2026-07-19 — plan authored from the verified diagnosis (`analysis.md`); ledger seeded, all WPs open.
- 2026-07-19 — orchestration started. Plan folder committed to `main` (`b0eb05a`) so worktrees carry the specs. Batch 1 dispatched: WP 0.1, 1.1, 1.2, 3.1 (file-disjoint bar the `.env.example` comment shared by 0.1/1.1 — 0.1 merges first, 1.1 rebases). Branch convention `wp/hub-fixes/<id>`.
- 2026-07-19 — WP 0.1 validated + merged to `main` (`03fd4f5`); ledger ticked. Diff = the 3 exclusive files; gate green (typecheck/test/lint); live scoped-Qlik proof queued for owner. WP 1.1/1.2/3.1 still running.
- 2026-07-19 — WP 3.1 validated + merged to `main` (`b52b314`); ledger ticked. Diff = the 4 exclusive web files; integrated typecheck + lint green (full test+build deferred to batch close). Owner-acceptance added: both-theme chip-contrast look. WP 1.1 (Opus) + 1.2 still running.
- 2026-07-19 — WP 1.2 validated + merged to `main` (`3afb49c`); ledger ticked. Shared additive-only (`toolScope`/`scopeMode`); `index.ts` builtins-honor is 8 lines (1.3 owns the rest of `resolveHubMcpGrants`); integrated typecheck green. Only WP 1.1 (Opus) still running — merges last (shares `.env.example` with 0.1), then full gate + `pnpm build` closes Batch 1.
- 2026-07-19 — **Batch 1 CLOSED.** WP 1.1 validated (full engine review: `session-service` builds deferred tools into the callable map; `turn-engine` `prepareStep` gates them; `tool-search` promotes token-capped, ungranted-safe) + merged to `main` (`44d2ff9`); the one anticipated `.env.example` conflict resolved to the `auto` doc (`c41d599` reconciled 0.1's now-stale runbook after 1.1/1.2). **Full batch gate GREEN on integrated `main`:** typecheck ✅ · API 2860/2860 + web 2417 pass/5 skip ✅ · lint ✅ · `pnpm build` ✅. All four WPs (0.1, 1.1, 1.2, 3.1) ticked.
  - **Pending owner decision (docker-compose):** WP 0.1's `HUB_TOOL_LOADING_DEFAULT: eager` override is LEFT IN PLACE (not removed) so the owner's first live validation uses the long-tested eager path. Now that WP 1.1 landed (`auto` + promotion), it is OPTIONAL — remove it to run on the `auto` default (which safely defers a large unscoped grant instead of blowing the budget). Owner's call; the live RC1/RC3 proof works under either mode.
  - **Follow-up carried to WP 1.3:** env-override threading for the two new caps (see the 1.3 line).
- 2026-07-20 — **Batch 12 CLOSED — WORKSTREAM COMPLETE (21/21).** WP 7.R (final adversarial review, SOLO) merged `b941b73`; all 7 invariant categories held, no defects, delta was test-only + docs (I re-verified: `safeHref` product code blocks `javascript:`/`data:`/`vbscript:`, the new probes assert blocking, no guard weakened). **Final full gate GREEN on complete `main`:** typecheck · API 2969/2969 · web 2516 pass/5 skip · lint · build. RC1–RC7 all fixed across 12 batches (10 parallel-worktree batches + no rework loops; every batch validated diff-by-diff + gate re-run by the orchestrator, never on agent faith). Orchestrator-authored integration wires (all typecheck+gate-covered): `index.ts` propose-path `mcpCatalog` (`1a3aa07`), `.env.example`/user-guide reconciliation, live board `debateRounds` (`adf7977`). One schema change: **migration v51** (WP 6.1, `hub_sessions.mode` widened for `auto`). **Live/browser proofs → [`owner-acceptance-walk.md`](./owner-acceptance-walk.md)** (nothing faked). Open owner items: scoped-Qlik call, eager-override removal decision, v51-aware deploy, both-theme walks, live `web.search`. **On local `main` only — not pushed; owner merges/pushes.**
- 2026-07-20 — **Batch 11 CLOSED** (WP 6.2, SOLO). Composer mode/autonomy clarity. **Phase 6 COMPLETE** (6.1 + 6.2). Merged `58a8269`; full gate GREEN (typecheck · API 2959/2959 · web 2510/5skip · lint · build). **20/21 done.** Only WP 7.R (adversarial review + owner-acceptance walk assembly) remains. Next: Batch 12 = **7.R** (final — Opus, review + assembly).
- 2026-07-20 — **Batch 10 CLOSED** (WP 6.1, SOLO). RC7 fixed: `auto` session mode + per-message chat-vs-mission routing + clarify card; the two queued ConversationPane follow-ups (1.3 notice, 2.3 subtitle) also cleared. **⚠️ Schema change:** this WP adds **migration v51** (the workstream's ONLY migration — conventions expected none, but `hub_sessions.mode`'s CHECK genuinely rejects `'auto'`; the widening is a tested, FK-safe, idempotent table rebuild — `LATEST_SCHEMA_VERSION` is now 51). Owner deploy runs it on the existing DB; standard migration — a pre-deploy backup is prudent but the rebuild is row-preserving + FK-checked. The agent was interrupted by an OAuth infra error mid-run and RESUMED (no work lost). Merged `d484f20`; full gate GREEN (typecheck · API 2956/2956 · web 2500/5skip · lint · build). **19/21 done — all 7 root causes (RC1–RC7) now addressed.** Remaining: 6.2 (composer clarity), 7.R (adversarial review + owner-acceptance assembly). Next: Batch 11 = **6.2 SOLO**, then Batch 12 = **7.R** (final).
- 2026-07-20 — **Batch 9 CLOSED** (WP 4.4, SOLO). Round-based debate (D-HF3). **Phase 4 COMPLETE** (4.1–4.4). Merged `91c2b9b` + a one-line integration fix (`adf7977`, live-graph `debateRounds`). Full gate GREEN (typecheck · API 2946/2946 · web 2494/5skip · lint · build). **18/21 done.** Remaining: 6.1 (orchestrator; carries 3 follow-ups), then 6.2, 7.R. Next: Batch 10 = **6.1 SOLO** (`auto` session mode + chat-vs-mission clarify card — orchestrator + turn-engine + ConversationPane + capabilities; owns e2e; carries: WP 1.3 transcript notice, WP 2.3 `parentScope` prop + `catalogServerIds`, rebase onto scrim commit).
- 2026-07-20 — **Batch 8 CLOSED** (WP 3.2, SOLO). Mission synthesis now runs through the turn engine with GenUI (D-HF4). **Phase 3 COMPLETE** (3.1 + 3.2). Merged `6e4e529`; full gate GREEN (typecheck · API 2940/2940 · web 2490/5skip · lint · build). **17/21 done.** Remaining: 4.4, 6.1 (orchestrator-serialized), then 6.2, 7.R. Next: Batch 9 = **4.4 SOLO** (round-based debate — orchestrator + topologies + topology-graph.ts).
- 2026-07-19 — **Batch 7 CLOSED** (WP 2.5, SOLO). Mission HITL (D-HF6) landed. **Phase 2 COMPLETE** (2.1–2.5): mission agents are now real tool-using sessions with inherited grants, real cost/budget, and autonomy-governed approvals. Merged `aea5ec1`; full gate GREEN (typecheck · API 2935/2935 · web 2490/5skip · lint · build). **16/21 done.** Remaining: 3.2, 4.4, 6.1 (orchestrator-serialized), then 6.2, 7.R. Next: Batch 8 = **3.2 SOLO** (synthesis through the turn engine w/ GenUI — session-service + orchestrator + synthesis; owns e2e per conventions).
- 2026-07-19 — **Batch 6 CLOSED** (WP 2.4 + 5.2, disjoint). 2.4 `8c5eaeb` (real mission cost + working trip + honest board spend), 5.2 `f295ec1` (research onboarding surfacing). Full gate GREEN (typecheck · API 2925/2925 · web 2485/5skip · lint · build) — note: heavy parallel-worktree load makes the full API/web suites intermittently flake (unrelated files, `metrics-perf`); verified by clean re-runs each time. **15/21 done.** Follow-ups queued: WP 6.1 gets 3 (1.3 transcript notice, 2.3 `parentScope` prop, its own ConversationPane rebase); `?addServer=research` App.tsx auto-open is a standalone owner follow-up. Remaining WPs (2.5, 3.2, 4.4, 6.1, 6.2, 7.R) are orchestrator-serialized → ~one per batch. Next: Batch 7 = **2.5 SOLO** (mission HITL — orchestrator + turn-engine).
- 2026-07-19 — **Batch 5 CLOSED** (WP 2.3, SOLO). Grant-inheritance (D-HF5) + the WP 2.2 propose-path carry-forward, both landed. Merged `e3f16ee`; I then wired the propose-path `mcpCatalog` dep at `index.ts` (`1a3aa07`) so RC2.4's prompt-planned grant proposal is LIVE (was stub-only). Full gate GREEN (typecheck · API 2922/2922 · web 2465/5skip · lint · build). **13/21 done.** Next: Batch 6 = **2.4 + 5.2** (disjoint: 2.4 = orchestrator/planner/topologies/board; 5.2 = MissionPlanCard/ConversationPane/presets, now unblocked by 2.2 + rebasing MissionPlanCard onto 2.3).
- 2026-07-19 — Concurrent-session gate resolved: owner confirmed the assistant-hub-ux agent is done; it committed its scrim WIP as `b746aa8` (the 4 hub UX files) cleanly atop my Batch-4 close — main tree now CLEAN, all hub-fixes commits intact + reachable. Remaining orchestrator-serialized WPs run one-per-batch (fillers 5.2/6.2/7.R are dependency-blocked). **Batch 5 = WP 2.3 SOLO.** (6.1's ConversationPane collision risk is gone — the WIP is committed; 6.1 stays late in sequence.)
- 2026-07-19 — **Batch 4 CLOSED** (WP 2.2, 4.3, 5.1). Integrated one at a time (each reviewed + typecheck): 4.3 `46aff07`, 2.2 `fc1a46d`, 5.1 `2415fe7`. **Full gate GREEN** on a CLEAN integrated worktree (typecheck · API 2906/2906 · web 2458/5skip · lint · build). ⚠️ **Concurrent-session hazard:** an assistant-hub-ux session left uncommitted WIP (transcript-scrim polish) in `main`'s working tree — `ConversationPane.tsx(+test)`, `EmptySessionIntro.tsx`, `lib/hub-ux.ts` (+ a "Waves 1-3" stash). None are hub-fixes files; left UNTOUCHED. The batch gate was therefore run in the (clean) 5.1 worktree fast-forwarded to main, not on the polluted main tree. The 5.1 agent also briefly `checkout -b`'d in the main checkout by mistake but self-reconciled — main verified intact (HEAD correct, 4.3+2.2 merges reachable). **12/21 done.** Next: Batch 5 = one orchestrator-WP + fillers (2.3 [+2.2 propose carry-forward] is the natural next; pair with 5.2 which is now unblocked by 2.2).
- 2026-07-19 — **Re-plan (from Files sections):** the README's "Batch 4 = 2.2·2.3" is invalid — 2.2 ∩ 2.3 both edit `MissionPlanCard.tsx` (2.3's own spec says run it AFTER 2.2). Also 2.2 ∩ 2.4 share `planner.ts`, 2.4 ∩ 4.3 share `MissionBoard.tsx`, and six WPs (2.3/2.4/2.5/3.2/4.4/6.1) all touch `orchestrator.ts` → one orchestrator-WP per batch. **Batch 4 = 2.2 + 4.3 + 5.1** (mutually disjoint; none touch `orchestrator.ts`). 2.3 → Batch 5 (rebases MissionPlanCard onto 2.2).
- 2026-07-19 — **Batch 3 CLOSED** (WP 2.1, SOLO). The linchpin RC2 fix landed: mission children are real tool-using sessions. Reviewed the full engine diff (production runner default, `runAgentTurn` seam, minimal approvals, additive turn-engine/shared, byte-compatible `structured` rollback); merged `d426068`, no conflicts. **Full gate GREEN** (typecheck · API 2884/2884 + web 2435/5skip · lint · build). Verified the WP4.4 hub-UI e2e failure reproduces on the pre-2.1 baseline → 2.1 exonerated; its own mission e2e passes. **Next: Batch 4 = WP 2.2 + 2.3** (planner catalog + plan-card grants; grant-inheritance rule + plan validation — 2.3 now unblocked by 2.1). 9/21 done.
- 2026-07-19 — **Batch 2 CLOSED** (WP 1.3, 1.4, 4.1, 4.2). Integrated one at a time (each diff reviewed + per-merge typecheck): 1.4 `34a9d28`, 4.1 `8121a76`, 4.2 `5084efa`, 1.3 `12b068b` (clean, no conflicts — the four were file-disjoint; only 1.3 touched shared). WP 1.3 also wired the WP 1.1 env-threading follow-up. **Full batch gate GREEN on integrated `main`:** typecheck ✅ · API 2879/2879 + web 2435 pass/5 skip ✅ · lint ✅ · `pnpm build` ✅. All four ticked. New follow-up carried to WP 6.1: render the (already-wired) `turn.mcpServerIssues` inline transcript notice in `ConversationPane.tsx`. **Next: Batch 3 = WP 2.1 SOLO** (turn-engine agent runner — touches `orchestrator.ts` + `session-service.ts` + e2e stubs; runs alone).
