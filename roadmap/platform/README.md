# Platform hardening — implementation plan · **PRIORITY: MEDIUM (rolling)**

Owner directive (2026-07-04): the cross-cutting operability items no feature plan owns —
onboarding, docs, diagnostics, upgrade safety, performance at fleet scale, and the consolidated
owner-acceptance walks. Rolling backlog: WPs here are independent and can interleave with any
workstream (they touch few serialized files).

Living state: [`STATUS.md`](./STATUS.md) (driven by `/next-wp platform`).

## WP index

| WP | Title | Depends on | Size |
|---|---|---|---|
| 1.1 | **First-run onboarding**: seeded demo content (bundled stub MCP server + sample scenario/test/graded suite) behind an explicit "load demo data" action; guided empty states on the main views | benchmarks P1 (for a graded example) | M |
| 1.2 | **Docs & changelog**: in-app docs route (markdown from `docs/`, served by the API like the SPA — no new dependency; first content: [`docs/skill-authoring.md`](../../docs/skill-authoring.md) with **stable heading anchors** — quality findings deep-link them, so anchor renames are breaking), `CHANGELOG.md` discipline wired into the release flow, per-view help links | — | M |
| 1.3 | **Diagnostics bundle**: one-click redacted export (versions, env sans secrets, DB table counts/sizes, recent error log, migration level) for bug reports; proven secret-free by test | — | S |
| 1.4 | **Upgrade-path test harness**: captured fixture DBs (pre-v13 and each later claimed migration) → migrate → invariant assertions; runs in the normal test suite so every future migration must keep it green | — | M |
| 1.5 | **Performance & scale pass**: generated fixtures at fleet scale (100 servers / 10K runs / 1M grade rows), budget assertions for the hot endpoints (scan list, run console load, suite analytics), index review with measured before/after | benchmarks P3 (grade volume) | L |
| 1.6 | **Owner-acceptance consolidation**: one runnable checklist collecting every pending owner walk across ALL ledgers (testing 4.1/4.4, skillflow visual walks, skill-ide phases, benchmarks phases) with exact URLs, fixtures, and steps — preparation only; the walks stay owner-only | — | S |

## Invariants

- Demo/seed data is clearly labeled, removable in one action, and never mixes with real data.
- The diagnostics bundle is proven secret-free by an automated test (same discipline as PAT
  redaction), not by review.
- Performance budgets are asserted numbers in tests, not aspirations in prose.
- 1.4's fixture DBs are committed binary artifacts — small, generated by script, documented.

## Definition of done (every WP)

Gate green from repo root + acceptance; ledger discipline per [`STATUS.md`](./STATUS.md).
