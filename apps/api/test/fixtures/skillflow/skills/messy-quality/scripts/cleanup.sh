#!/usr/bin/env bash
# A stored, NEVER-executed fixture script. Referenced without an exit-code/verify contract on
# purpose so the quality engine's `script-undocumented` rule fires.
echo "tidying the workspace"
