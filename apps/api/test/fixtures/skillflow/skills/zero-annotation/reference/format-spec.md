# Report format spec

The generated report must contain these sections, in this order:

1. **Title** — one line, the source file name.
2. **Summary** — record count and any rows that were skipped during validation.
3. **Records** — a table of the validated rows.
4. **Notes** — free text, optional.

Do not add extra top-level sections; nested detail belongs under Notes.
