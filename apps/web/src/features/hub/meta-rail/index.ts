// Assistant Hub UX (WP1.2, D-HUX3) — the meta rail's public surface. WP1.1 imports `MetaRail` from
// here (or directly from `./MetaRail`) into `AssistantView.tsx`'s `metaRail` slot at wave merge.
export { MetaRail } from "./MetaRail";
export type {
  HubOutputFileEntry,
  MetaRailContextProps,
  MetaRailOutputsProps,
  MetaRailProgressProps,
  MetaRailProps,
  MetaRailSectionId,
} from "./MetaRail";
